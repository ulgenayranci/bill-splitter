---
phase: quick-260624-j9b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/wizard/AssignItemsStep.tsx
  - components/wizard/ResultsStep.tsx
  - components/wizard/AddItemsStep.tsx
  - components/wizard/AddPeopleStep.tsx
  - components/wizard/DisambiguationDialog.tsx
  - components/wizard/ShareLinkButton.tsx
  - components/wizard/OcrErrorToast.tsx
  - components/split/UnclaimedBanner.tsx
  - app/api/clarify/route.ts
  - __tests__/AssignItemsStep.test.tsx
  - __tests__/ResultsStep.test.tsx
  - __tests__/AddItemsStep.test.tsx
  - __tests__/AddPeopleStep.test.tsx
  - __tests__/DisambiguationDialog.test.tsx
  - __tests__/ShareLinkButton.test.tsx
  - __tests__/UnclaimedBanner.test.tsx
  - app/providers.tsx
  - lib/createSession.ts
  - lib/sessionUtils.ts
autonomous: true
requirements: [CLEANUP-V2]

must_haves:
  truths:
    - "Retired wizard components and their tests no longer exist in the tree"
    - "UnclaimedBanner, OcrErrorToast, and the /api/clarify route no longer exist"
    - "Providers renders children directly with no Toast.Provider wrapper or OcrErrorToast import"
    - "Stale comments in createSession.ts and sessionUtils.ts no longer reference deleted components"
    - "tsc --noEmit is clean, the vitest suite is green, and the production build passes"
  artifacts:
    - path: "app/providers.tsx"
      provides: "Simplified provider tree rendering children directly"
      contains: "children"
  key_links:
    - from: "app/providers.tsx"
      to: "children"
      via: "direct render (no Toast.Provider wrapper)"
      pattern: "return.*children"
---

<objective>
Delete v2.0 dead code identified by the milestone audit: the retired wizard
step components, the UnclaimedBanner, the OcrErrorToast, the /api/clarify route,
and the seven tests that covered only those components. Then clean up the two
runtime/comment references that point at the deleted code.

Purpose: Remove dead code that no live runtime path imports, shrinking the
surface area and eliminating confusing stale references after the Phase 9-11
unification on the collaborative /split flow.

Output: A tree with the dead files gone, a simplified Providers, corrected
comments, and a green tsc/test/build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- The verified import graph (built by the orchestrator) is LOCKED. -->
<!-- No live runtime path imports any of the DELETE files. -->
<!-- providers.tsx is the ONLY runtime Toast user; after removing OcrErrorToast -->
<!-- the Toast.Provider wraps nothing that emits, so drop it entirely. -->

Current app/providers.tsx:
- imports `Toast` from `@base-ui/react/toast`
- imports `OcrErrorToast` from `@/components/wizard/OcrErrorToast`
- wraps `{children}` in `<Toast.Provider>` alongside `<OcrErrorToast />`

After this plan, providers.tsx must render `{children}` directly:
- no `Toast` import, no `OcrErrorToast` import
- `Providers` returns `children` (still a `'use client'` component taking `{ children }`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead components, route, and their tests</name>
  <files>
    components/wizard/AssignItemsStep.tsx,
    components/wizard/ResultsStep.tsx,
    components/wizard/AddItemsStep.tsx,
    components/wizard/AddPeopleStep.tsx,
    components/wizard/DisambiguationDialog.tsx,
    components/wizard/ShareLinkButton.tsx,
    components/wizard/OcrErrorToast.tsx,
    components/split/UnclaimedBanner.tsx,
    app/api/clarify/route.ts,
    __tests__/AssignItemsStep.test.tsx,
    __tests__/ResultsStep.test.tsx,
    __tests__/AddItemsStep.test.tsx,
    __tests__/AddPeopleStep.test.tsx,
    __tests__/DisambiguationDialog.test.tsx,
    __tests__/ShareLinkButton.test.tsx,
    __tests__/UnclaimedBanner.test.tsx
  </files>
  <action>
    Delete the eight dead components, the /api/clarify route, and the seven
    tests listed in <files> — the import graph (LOCKED, built by the
    orchestrator) confirms no live runtime path imports any of them. Use
    `git rm` so the deletions are staged.

    After removing app/api/clarify/route.ts, remove the now-empty
    app/api/clarify/ directory.

    Do NOT touch the surviving wizard files (WizardShell, SetupStep, AppHeader,
    ProgressStrip, BillPhotoLightbox, OcrLoadingOverlay), the live /split flow,
    lib/billMath.ts, app/api/session/** routes, stores/useBillStore.ts, or
    vitest.setup.ts. Do NOT touch __tests__/CollaborativeClaimingView.test.tsx —
    its only UnclaimedBanner reference is a test title asserting ABSENCE via DOM
    query; it does not import the component and stays green.

    Commit as: `chore(quick-260624-j9b): delete v2.0 dead components, route, and tests`
  </action>
  <verify>
    <automated>test ! -e components/wizard/OcrErrorToast.tsx && test ! -e components/split/UnclaimedBanner.tsx && test ! -e app/api/clarify && test ! -e __tests__/UnclaimedBanner.test.tsx && echo OK</automated>
  </verify>
  <done>All 16 dead files (8 components, 1 route, 7 tests) are deleted and staged; the app/api/clarify/ directory is gone; no surviving file was touched.</done>
</task>

<task type="auto">
  <name>Task 2: Remove runtime mount and fix stale comments</name>
  <files>app/providers.tsx, lib/createSession.ts, lib/sessionUtils.ts</files>
  <action>
    In app/providers.tsx: remove both the `Toast` import (from
    `@base-ui/react/toast`) and the `OcrErrorToast` import. Since providers.tsx
    is the only runtime Toast user and OcrErrorToast was the only thing emitting
    into the provider, drop the `<Toast.Provider>` wrapper and the
    `<OcrErrorToast />` mount entirely. The `Providers` component must remain a
    `'use client'` component taking `{ children }` and now render `children`
    directly (e.g. return a fragment or `children`).

    In lib/createSession.ts (the comment near line 16): the text reads "Used by
    both SetupStep and ShareLinkButton" — drop the "and ShareLinkButton" mention
    so it reads as used by SetupStep only.

    In lib/sessionUtils.ts (the comment near line 6): the text reads "Shared
    helper used by CollaborativeClaimingView, UnclaimedBanner, and
    PersonResultsScreen" — drop the "UnclaimedBanner," mention so it lists
    CollaborativeClaimingView and PersonResultsScreen only.

    These are comment-only edits in the two lib files; do not change any code,
    signatures, or exports there.

    Commit as: `chore(quick-260624-j9b): drop OcrErrorToast mount and fix stale comments`
  </action>
  <verify>
    <automated>! grep -q "OcrErrorToast" app/providers.tsx && ! grep -q "Toast.Provider" app/providers.tsx && ! grep -q "ShareLinkButton" lib/createSession.ts && ! grep -q "UnclaimedBanner" lib/sessionUtils.ts && echo OK</automated>
  </verify>
  <done>Providers renders children directly with no Toast/OcrErrorToast references; the two stale comments no longer name deleted components.</done>
</task>

<task type="auto">
  <name>Task 3: Verify tsc, test suite, and build</name>
  <files>(verification only — no file changes)</files>
  <action>
    Run the three acceptance gates on the main tree:
    1. `npx tsc --noEmit` — must be clean (no type errors).
    2. `npx vitest run` — the suite must be green; the 7 deleted test files must
       be gone from the run. Note: the project had documented PRE-EXISTING
       failures in the AddPeopleStep/AddItemsStep wizard tests — those test files
       are deleted in Task 1, which removes those failures. A lower total test
       count post-cleanup is expected and correct; do not treat it as a regression.
    3. `npm run build` (Next.js build) — must pass.

    If tsc or build surfaces a reference to a deleted file that the LOCKED import
    graph did not anticipate, fix the offending importer minimally (it would be a
    genuine stale reference), re-run the gate, and note it in the summary. Do not
    expand scope beyond removing the dead reference.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run build</automated>
  </verify>
  <done>tsc --noEmit is clean, vitest run is green with the 7 deleted test files absent, and the Next.js build passes.</done>
</task>

</tasks>

<verification>
- 8 components, 1 route, and 7 tests deleted; app/api/clarify/ directory removed
- app/providers.tsx renders children directly (no Toast.Provider, no OcrErrorToast)
- Stale comments in createSession.ts and sessionUtils.ts corrected
- `npx tsc --noEmit` clean
- `npx vitest run` green (7 deleted test files absent; lower count expected)
- `npm run build` passes
</verification>

<success_criteria>
- All 16 dead files removed and the empty clarify directory gone
- No surviving live file references a deleted component
- Providers simplified; stale comments fixed
- All three acceptance gates (tsc, vitest, build) pass
</success_criteria>

<output>
Create `.planning/quick/260624-j9b-cleanup-v2-dead-code/260624-j9b-SUMMARY.md` when done
</output>
