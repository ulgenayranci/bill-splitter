---
phase: quick-260623-u9k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/ulgenayranci/playground/gsd-course/components/ui/button.tsx
  - /Users/ulgenayranci/playground/gsd-course/app/split/[sessionId]/CollaborativeClaimingView.tsx
  - /Users/ulgenayranci/playground/gsd-course/components/wizard/AddItemsStep.tsx
  - /Users/ulgenayranci/playground/gsd-course/components/wizard/SetupStep.tsx
  - /Users/ulgenayranci/playground/gsd-course/components/wizard/AddPeopleStep.tsx
  - /Users/ulgenayranci/playground/gsd-course/components/split/PersonSlotPicker.tsx
autonomous: true
---

<objective>
Implement the easy billsy spec §06 BUTTON component design so buttons have a real fill.
The prior pass left outline/icon buttons on `bg-background` (paper) — same color as the page,
so they read as empty. Match the spec's button types: outline = WHITE fill + ink + n200 border;
primary = coral + coral drop-shadow; ghost = n100 fill + n500 text.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: button.tsx variant fills (spec §06)</name>
  <files>/Users/ulgenayranci/playground/gsd-course/components/ui/button.tsx</files>
  <action>
In the cva `variants.variant`:
- default (primary): keep `bg-primary text-primary-foreground`; ADD the coral drop-shadow `shadow-[0_4px_14px_rgba(241,96,63,0.28)]` (spec --eb-sh-btn). Resting coral, no hover.
- outline: replace `bg-background` with `bg-white`; add `text-foreground`; keep `border-border` (1.5px ok). KEEP the existing dark: overrides (`dark:bg-input/30 dark:border-input`) and the `aria-expanded:` states.
- ghost: give it the spec fill — `bg-muted text-muted-foreground` (instead of transparent). Keep its `aria-expanded:` states.
- secondary: leave as-is.
Do NOT add any `hover:` utilities. Keep `active:`/`focus-visible:`.
  </action>
  <verify>
    <automated>cd /Users/ulgenayranci/playground/gsd-course && grep -q "bg-white" components/ui/button.tsx && grep -q "rgba(241,96,63" components/ui/button.tsx && grep -q "bg-muted text-muted-foreground" components/ui/button.tsx && ! grep -q "hover:" components/ui/button.tsx && echo PASS</automated>
  </verify>
  <done>outline variant is white-filled + ink + border; primary has the coral shadow; ghost has n100 fill; no hover.</done>
</task>

<task type="auto">
  <name>Task 2: custom icon buttons → white fill + build</name>
  <files>/Users/ulgenayranci/playground/gsd-course/app/split/[sessionId]/CollaborativeClaimingView.tsx, /Users/ulgenayranci/playground/gsd-course/components/wizard/AddItemsStep.tsx, /Users/ulgenayranci/playground/gsd-course/components/wizard/SetupStep.tsx, /Users/ulgenayranci/playground/gsd-course/components/wizard/AddPeopleStep.tsx, /Users/ulgenayranci/playground/gsd-course/components/split/PersonSlotPicker.tsx</files>
  <action>
In the 13 custom icon buttons currently styled `... border border-border bg-background text-zinc-*`, replace `bg-background` with `bg-white` (keep `border border-border` and the existing text color). Use `grep -rn "border border-border bg-background" app components` for the live list. Do NOT touch the dashed "Add item"/scan controls, ClaimableItemCard (it uses variant="outline"), or button sizes.
  </action>
  <verify>
    <automated>cd /Users/ulgenayranci/playground/gsd-course && ! grep -rn "border border-border bg-background" app components && npm run build 2>&1 | tail -3 && echo BUILD_DONE</automated>
  </verify>
  <done>all 13 icon buttons use bg-white; `grep "border border-border bg-background"` empty; build passes.</done>
</task>

</tasks>

<output>
Create .planning/quick/260623-u9k-implement-spec-button-component-design-o/260623-u9k-SUMMARY.md when done.
</output>
