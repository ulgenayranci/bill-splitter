---
phase: 01-manual-bill-splitter
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - components/wizard/SetTipStep.tsx
  - components/wizard/ResultsStep.tsx
  - __tests__/SetTipStep.test.tsx
  - __tests__/ResultsStep.test.tsx
autonomous: true
requirements:
  - TIP-01
  - RESULTS-01
must_haves:
  truths:
    - "User can pick 15%, 18%, 20%, or Custom tip and see the dollar tip amount update live"
    - "User can advance to Results from any tip selection (including 0%)"
    - "Results screen shows one card per person with their total in 28px amber-600 display text by default (D-03)"
    - "Tapping a result card expands it inline to reveal item-by-item shares + tip share + line total (D-03)"
    - "Only one card can be expanded at a time"
    - "Bottom strip on Results shows total bill amount (subtotal + full tip)"
    - "Per-person totals sum exactly equals subtotal + tip (cents conservation invariant)"
    - "Results back button returns to Tip step (Step 4) so user can adjust tip and see totals re-render"
  artifacts:
    - path: "components/wizard/SetTipStep.tsx"
      provides: "Step 4 UI: 15/18/20/Custom tip selector with live tip amount"
      min_lines: 60
    - path: "components/wizard/ResultsStep.tsx"
      provides: "Step 5 UI: collapsible per-person result cards + total bill strip"
      min_lines: 80
    - path: "__tests__/SetTipStep.test.tsx"
      provides: "Component tests for TIP-01 (preset selection, custom tip, live amount)"
    - path: "__tests__/ResultsStep.test.tsx"
      provides: "Component tests for RESULTS-01 (per-person totals, expand/collapse, total bill)"
  key_links:
    - from: "components/wizard/SetTipStep.tsx"
      to: "stores/useBillStore.ts"
      via: "setTipPercent + items + setStep(5)"
      pattern: "setTipPercent"
    - from: "components/wizard/SetTipStep.tsx"
      to: "lib/billMath.ts"
      via: "computeSubtotalCents + computeTipCents + formatCents for live tip display"
      pattern: "computeTipCents|formatCents"
    - from: "components/wizard/ResultsStep.tsx"
      to: "lib/billMath.ts"
      via: "computePersonTotals + formatCents for per-person totals"
      pattern: "computePersonTotals"
    - from: "components/wizard/ResultsStep.tsx"
      to: "stores/useBillStore.ts"
      via: "people, items, assignments, tipPercent — read-only consumers"
      pattern: "useBillStore"
---

<objective>
Vertical slice 3: tip selection and final results breakdown. After this plan, the entire Phase 1 wizard is fully functional end-to-end. Users walk through 5 steps and arrive at a Results screen showing each person's owed amount with expandable item details.

Purpose: Closes Phase 1. TIP-01 and RESULTS-01 both addressed end-to-end. The bill splitter ships as a complete vertical product even before OCR or sharing arrive in later phases.

Output: `SetTipStep` and `ResultsStep` are fully implemented (no longer placeholders). The cents-conservation invariant from `computePersonTotals` is exercised by the UI — sums always equal subtotal + tip exactly. D-01, D-02, and D-03 all enforced in the code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-manual-bill-splitter/01-CONTEXT.md
@.planning/phases/01-manual-bill-splitter/01-RESEARCH.md
@.planning/phases/01-manual-bill-splitter/01-UI-SPEC.md
@.planning/phases/01-manual-bill-splitter/01-VALIDATION.md
@.planning/phases/01-manual-bill-splitter/01-02-SUMMARY.md
@stores/useBillStore.ts
@lib/billMath.ts
@components/wizard/AddPeopleStep.tsx
@components/wizard/AssignItemsStep.tsx
@components/ui/button.tsx
@components/ui/input.tsx
@components/ui/card.tsx
@components/ui/separator.tsx

<interfaces>
<!-- Consumed from Plan 01 (already exist). -->

`stores/useBillStore.ts`:
```typescript
const tipPercent = useBillStore((s) => s.tipPercent)  // default 18
const setTipPercent = useBillStore((s) => s.setTipPercent)
const items = useBillStore((s) => s.items)
const people = useBillStore((s) => s.people)
const assignments = useBillStore((s) => s.assignments)
const setStep = useBillStore((s) => s.setStep)
```

`lib/billMath.ts`:
```typescript
computeSubtotalCents(items: Item[]): number
computeTipCents(subtotalCents: number, tipPercent: number): number
computePersonTotals(people, items, assignments, tipPercent): Record<PersonId, number>
formatCents(cents: number): string
```

INVARIANT (verified by Plan 01 Test 19): `Object.values(computePersonTotals(...)).reduce((s,c) => s+c, 0) === subtotalCents + tipCents` exactly. The Results UI must NOT do its own arithmetic — it consumes `computePersonTotals` directly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement SetTipStep with preset/custom tip selection + live amount</name>
  <files>
    components/wizard/SetTipStep.tsx,
    __tests__/SetTipStep.test.tsx
  </files>
  <read_first>
    - components/wizard/AddPeopleStep.tsx (CTA pattern, layout)
    - components/wizard/AssignItemsStep.tsx (back+forward CTA row pattern)
    - stores/useBillStore.ts (tipPercent default = 18, setTipPercent signature)
    - lib/billMath.ts (computeSubtotalCents, computeTipCents, formatCents)
    - .planning/phases/01-manual-bill-splitter/01-UI-SPEC.md (Step 4 — Set Tip section: presets 15/18/20/Custom, default 18, "Tip: $X.XX" live line, equal-split note D-02)
    - .planning/phases/01-manual-bill-splitter/01-CONTEXT.md (D-01 tip enters at end, D-02 equal split)
  </read_first>
  <behavior>
    `__tests__/SetTipStep.test.tsx` covers TIP-01:

    Pre-seed pattern:
    ```typescript
    beforeEach(() => {
      useBillStore.getState().reset()
      useBillStore.getState().addPerson('Alice')
      useBillStore.getState().addItem('Pizza', 1000)  // $10.00
    })
    ```

    - Test 1: Renders 4 buttons with text "15%", "18%", "20%", "Custom"
    - Test 2: On initial render, 18% button is visually selected (has accent class — assert via `data-state` attribute or `aria-pressed="true"` or className containing `bg-amber-600`)
    - Test 3: Tip dollar line shows "$1.80" by default ($10 × 18%)
    - Test 4: Click "20%" → setTipPercent(20) called; useBillStore.getState().tipPercent === 20; tip line shows "$2.00"
    - Test 5: Click "15%" → tipPercent === 15; tip line shows "$1.50"
    - Test 6: Click "Custom" → reveals an Input field; previously hidden when not on custom
    - Test 7: Type "25" into custom input → tipPercent === 25; tip line shows "$2.50"
    - Test 8: Type "0" into custom input → tipPercent === 0; tip line shows "$0.00"
    - Test 9: Type non-numeric "abc" into custom input → tipPercent unchanged from previous valid value (rejected silently or with inline error — Claude's discretion; test asserts no crash and a valid number remains in store)
    - Test 10: "See results" CTA is always enabled (no disabled attribute)
    - Test 11: Click "See results" → useBillStore.getState().step === 5
    - Test 12: Click Back → useBillStore.getState().step === 3
  </behavior>
  <action>
    Two commits — RED then GREEN.

    Commit A (`test(01-03): add failing tests for SetTipStep`):

    Create `__tests__/SetTipStep.test.tsx` covering the 12 behaviors above. Run → RED. Commit.

    Commit B (`feat(01-03): implement SetTipStep with preset and custom tip selection`):

    Replace the placeholder `components/wizard/SetTipStep.tsx`. Required structure:

    1. `'use client'` directive on line 1.

    2. Heading: "Set tip" (20px semibold). Subtitle (14px label, zinc-500): "Pick a preset or enter a custom percent."

    3. Subtotal line above the buttons (helps user contextualize): "Subtotal: $X.XX" computed from `formatCents(computeSubtotalCents(items))`.

    4. Preset button row — 4 shadcn `Button`s side-by-side using `flex gap-2`:
       - Labels: "15%", "18%", "20%", "Custom"
       - Each button is `h-12` (48px), `flex-1` to evenly distribute
       - The selected button has variant="default" with class `bg-amber-600 hover:bg-amber-700 text-white` (accent fill)
       - Unselected buttons use variant="outline"
       - State: a local `mode: 'preset' | 'custom'` plus the store's `tipPercent` determines which is selected
       - Clicking a preset → call `setTipPercent(15|18|20)` AND set `mode = 'preset'`; collapse custom field
       - Clicking "Custom" → set `mode = 'custom'`; reveal custom Input below

    5. Custom Input (revealed only when mode === 'custom'):
       - shadcn `Input`, `inputMode="decimal"`, placeholder "Enter percent"
       - On `onChange`, parse the value: if matches `/^\d+(\.\d{1,2})?$/` and parseFloat ≤ 999, call `setTipPercent(parseFloat(value))`. If invalid, do not call (keep prior valid percent in store).
       - Suffix the visible field with a "%" label (use a wrapper `div` with `relative` positioning, or shadcn `Input` followed by an absolutely positioned span — pick whichever is cleaner)
       - Defaults to current `tipPercent` value when revealed for the first time

    6. Live tip amount line — below the buttons + custom input:
       - Computed via `formatCents(computeTipCents(computeSubtotalCents(items), tipPercent))`
       - Renders as: `Tip: $X.XX` (16px body, with the dollar amount in font-semibold)
       - Updates on every render — driven by store state, not local state

    7. Equal-split note (D-02 transparency): Below the tip line, in 14px label muted text: "Tip splits equally among ${people.length} people — ${formatCents(Math.floor(tipCents / people.length))} each (display only)" — informational only, no input. If `people.length === 0`, show simply "Tip splits equally".

    8. Bottom CTA row: `<Button variant="outline" onClick={() => setStep(3)} className="h-12 flex-1">Back</Button>` and `<Button onClick={() => setStep(5)} className="h-12 flex-1 bg-amber-600 hover:bg-amber-700">See results</Button>`. Always enabled.

    9. Layout: heading + subtotal at top, preset row in upper third (keyboard avoidance for the custom Input), CTA row at bottom with `pb-[env(safe-area-inset-bottom,16px)]`.

    10. Selected-state determination logic:
        ```typescript
        const PRESETS = [15, 18, 20] as const
        const isPresetMatch = mode === 'preset' && PRESETS.includes(tipPercent as 15 | 18 | 20)
        // selected preset: if isPresetMatch, the button matching tipPercent is highlighted; else none of the presets are.
        // "Custom" highlighted when mode === 'custom'.
        ```

    Run `npm run test -- --run`. Full suite passes — no regressions. Commit.
  </action>
  <verify>
    <automated>npm run test -- --run __tests__/SetTipStep.test.tsx &amp;&amp; npm run test -- --run &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `components/wizard/SetTipStep.tsx` exists, starts with `'use client'` (grep line 1)
    - File imports `setTipPercent`, `setStep`, `tipPercent`, `items`, `people` from useBillStore (grep: `setTipPercent`)
    - File imports `computeSubtotalCents`, `computeTipCents`, `formatCents` from billMath (grep: `computeTipCents`, `computeSubtotalCents`)
    - File contains all 4 button labels: "15%", "18%", "20%", "Custom" (grep -F for each)
    - File contains the live tip line text "Tip:" (grep -F: `Tip:`)
    - File contains `inputMode="decimal"` for the custom input (grep -F: `inputMode="decimal"`)
    - File calls `setStep(5)` from "See results" button (grep: `setStep(5)`)
    - File calls `setStep(3)` from Back button (grep: `setStep(3)`)
    - File has the equal-split note text "splits equally" (grep: `splits equally`)
    - `__tests__/SetTipStep.test.tsx` exists with ≥ 12 test cases (grep -c `it(` ≥ 12)
    - `npm run test -- --run __tests__/SetTipStep.test.tsx` exits 0 with ≥ 12 passing tests
    - Full test suite: `npm run test -- --run` exits 0 with ≥ 73 total passing tests (61 + 12)
    - `npx tsc --noEmit` exits 0
    - Two commits prefixed `test(01-03):` and `feat(01-03):` for this task
  </acceptance_criteria>
  <done>
    Step 4 of the wizard is fully functional. User picks a tip preset or types a custom percent, sees the live dollar amount update, and advances to Step 5. TIP-01 satisfied end-to-end. D-01 (tip enters at end) and D-02 (equal split, display note) both enforced.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ResultsStep with collapsible per-person breakdown + total bill strip</name>
  <files>
    components/wizard/ResultsStep.tsx,
    __tests__/ResultsStep.test.tsx
  </files>
  <read_first>
    - components/wizard/AddPeopleStep.tsx (avatar circle pattern with AVATAR_COLORS)
    - components/wizard/AssignItemsStep.tsx (Card pattern, formatCents usage)
    - stores/useBillStore.ts (read-only consumption of people, items, assignments, tipPercent)
    - lib/billMath.ts (`computePersonTotals`, `computeSubtotalCents`, `computeTipCents`, `formatCents`)
    - .planning/phases/01-manual-bill-splitter/01-UI-SPEC.md (Step 5 — Results section: 28px Display amber-600 totals, expandable cards, only one expanded at a time, fixed bottom strip with total bill, no CTA)
    - .planning/phases/01-manual-bill-splitter/01-CONTEXT.md (D-03: total visible by default, items revealed on tap)
    - .planning/phases/01-manual-bill-splitter/01-RESEARCH.md (Pattern 2 cents conservation, Pitfall 2 largest-remainder)
  </read_first>
  <behavior>
    `__tests__/ResultsStep.test.tsx` covers RESULTS-01:

    Pre-seed scenario A — solo + shared with conservation invariant:
    ```typescript
    // Alice owes Pizza ($10) split with Bob; Carol owes Coke ($2.50) solo
    // Tip 20% on subtotal $12.50 = $2.50 → split equally 3 ways = 84+83+83 cents
    // Expected:
    //   Alice: 500 (half of 1000) + 84 (tip share) = 584
    //   Bob:   500 + 83 = 583
    //   Carol: 250 + 83 = 333
    //   Sum: 584+583+333 = 1500 cents = subtotal 1250 + tip 250 ✓
    ```

    - Test 1: Renders one Card per person; assert 3 cards (Alice, Bob, Carol)
    - Test 2: Each card shows the person's total formatted via `formatCents`. Alice: "$5.84", Bob: "$5.83", Carol: "$3.33" — these specific strings appear in the rendered output
    - Test 3 (cents conservation): Sum of computePersonTotals values displayed across all cards equals `formatCents(subtotal + tip) === "$15.00"`. Verify by summing the per-person dollar amounts displayed and asserting equality — OR compute `computePersonTotals` in test and assert sum = 1500.
    - Test 4: Bottom strip shows "Total bill: $15.00" (subtotal $12.50 + 20% tip $2.50)
    - Test 5: Tap Alice's card → card expands and shows item details ("Pizza" + her share "$5.00") and tip share ("Tip: $0.84") and line total ("Total: $5.84")
    - Test 6 (single-expand rule): Tap Alice (expanded), then tap Bob → Alice collapses, Bob expands. Only ONE card has expanded content visible at a time.
    - Test 7: Tap an already-expanded card → it collapses (no expanded content visible)
    - Test 8: Per-person total displayed in a 28px-class element with amber-600 text (verify via `className` containing `text-[28px]` or equivalent and `text-amber-600`)
    - Test 9: Card expansion shows tip share line text "Tip:" (grep)
    - Test 10: Back button calls `setStep(4)` (no other CTA on this terminal screen)
    - Test 11: When a person has no items assigned, their card still renders, total = their tip share only (e.g., add a 4th person with no assignments → card shows their tip share, no item lines in expanded view)
  </behavior>
  <action>
    Two commits — RED then GREEN.

    Commit A (`test(01-03): add failing tests for ResultsStep`):

    Create `__tests__/ResultsStep.test.tsx`. The test setup pre-seeds the scenario above. Use precise expected values — `formatCents` of computed totals — to lock in the cents-conservation invariant. Compute expected per-person amounts in the test using the same `computePersonTotals` function (this is acceptable since the tests in Plan 01 already verified that function in isolation).

    Run → RED. Commit.

    Commit B (`feat(01-03): implement ResultsStep with collapsible per-person breakdown`):

    Replace the placeholder `components/wizard/ResultsStep.tsx`. Required structure:

    1. `'use client'` directive on line 1.

    2. Heading: "Here's what each person owes" (20px semibold) at top.

    3. Compute totals once per render:
       ```typescript
       const totals = computePersonTotals(people, items, assignments, tipPercent)
       const subtotalCents = computeSubtotalCents(items)
       const tipCents = computeTipCents(subtotalCents, tipPercent)
       const totalBillCents = subtotalCents + tipCents
       ```

    4. Local state: `const [expandedPersonId, setExpandedPersonId] = useState<PersonId | null>(null)`

    5. Per-person card list — for each person:
       - shadcn `Card` with onClick toggling expand: clicking sets `expandedPersonId` to this person if currently null or someone else, OR back to null if this person is already expanded (single-expand rule)
       - Header row inside card:
         - Avatar circle 40px with `AVATAR_COLORS[person.colorIndex]` background and first-letter initial
         - Name (20px heading) + total displayed in `text-[28px] font-semibold text-amber-600` (28px Display per UI-SPEC). Total comes from `formatCents(totals[person.id] ?? 0)`.
         - Chevron icon (lucide `ChevronDown`) that rotates 180° (`rotate-180` class) when this card is expanded
       - Expanded content (only when `expandedPersonId === person.id`):
         - Iterate over items, for each item where assignments[item.id]?.includes(person.id):
           - Compute this person's share: `Math.floor(item.priceCents / sharers.length)` for the base, plus the largest-remainder bonus if applicable. Easier: re-derive via `personItemShare(item, person.id, assignments)` helper:
             ```typescript
             function personItemShare(item: Item, personId: PersonId, assignments: Record<ItemId, PersonId[]>): number {
               const sharers = assignments[item.id] ?? []
               if (!sharers.includes(personId)) return 0
               const base = Math.floor(item.priceCents / sharers.length)
               const remainder = item.priceCents % sharers.length
               const idx = sharers.indexOf(personId)
               return base + (idx < remainder ? 1 : 0)
             }
             ```
             Place this helper INSIDE this file (not in `lib/billMath.ts` — it's a UI-display helper that uses the same largest-remainder math as `computePersonTotals`).
           - Render `<div>{item.name}</div><div>{formatCents(share)}</div>` (item name left, share right, 14px label per UI-SPEC)
         - Then, separately, the tip share line: compute as `Math.floor(tipCents / people.length) + (personIndex < (tipCents % people.length) ? 1 : 0)`. Render: `Tip: {formatCents(tipShare)}`.
         - Then a `<Separator />` and a "Total: {formatCents(totals[person.id])}" line in font-semibold.

    6. Bottom strip — fixed bottom element across the viewport:
       - Background: `bg-zinc-100` (light) / `bg-zinc-900` (dark via `dark:`)
       - Padding: `p-4 pb-[env(safe-area-inset-bottom,16px)]`
       - Text: `Total bill: {formatCents(totalBillCents)}` in font-semibold

    7. Back button: `<Button variant="outline" onClick={() => setStep(4)}>Back to tip</Button>` placed at the top-left of the card list area (so user can tap it before reaching the bottom strip). 48px height.

    8. No forward CTA — terminal step per UI-SPEC.

    9. Use `'use client'` directive.

    10. Subscribe with selectors: separate selector calls for each store slice.

    Run `npm run test -- --run`. Full suite passes — no regressions. Commit.
  </action>
  <verify>
    <automated>npm run test -- --run __tests__/ResultsStep.test.tsx &amp;&amp; npm run test -- --run &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `components/wizard/ResultsStep.tsx` exists, starts with `'use client'` (grep line 1)
    - File imports `computePersonTotals`, `computeSubtotalCents`, `computeTipCents`, `formatCents` from billMath (grep for each)
    - File imports `AVATAR_COLORS` and reads `people, items, assignments, tipPercent, setStep` from useBillStore (grep: `AVATAR_COLORS`, `useBillStore`)
    - File contains the heading text "Here's what each person owes" (grep -F: `Here&apos;s what each person owes` OR `Here's what each person owes` depending on entity escape)
    - File contains the bottom strip text "Total bill:" (grep -F: `Total bill:`)
    - File contains the per-item Tip share line "Tip:" inside the expanded view (grep -F: `Tip:`)
    - File contains the helper `personItemShare` with largest-remainder logic (grep: `personItemShare`, `Math.floor(item.priceCents / sharers.length)`)
    - File uses `text-[28px]` and `text-amber-600` for the per-person total class (grep -F: `text-[28px]`, `text-amber-600`)
    - File uses single-expand state: `expandedPersonId` (grep: `expandedPersonId`)
    - File calls `setStep(4)` for Back button (grep: `setStep(4)`)
    - File does NOT contain `setStep(6)` or any forward navigation (grep -v should confirm absence)
    - `__tests__/ResultsStep.test.tsx` exists with ≥ 11 test cases (grep -c `it(` ≥ 11)
    - `__tests__/ResultsStep.test.tsx` asserts the cents-conservation invariant: sum of `computePersonTotals` === subtotal + tip (grep: assertion comparing total bill to sum of person totals)
    - `npm run test -- --run __tests__/ResultsStep.test.tsx` exits 0 with ≥ 11 passing tests
    - Full test suite: `npm run test -- --run` exits 0 with ≥ 84 total passing tests (73 + 11)
    - `npx tsc --noEmit` exits 0
    - `npm run build` succeeds (proves the entire 5-step wizard production-builds without error)
    - Two commits prefixed `test(01-03):` and `feat(01-03):` for this task
  </acceptance_criteria>
  <done>
    Step 5 of the wizard is fully functional. After the user completes Steps 1-4, the Results screen shows one card per person with their total in 28px amber text. Tapping a card expands it inline to show item-by-item shares + tip share + line total. Only one card expands at a time. The bottom strip shows the total bill. The Back button returns to Step 4 so the user can adjust tip and see totals re-render. Cents-conservation invariant holds: sum of per-person totals equals subtotal + tip exactly. RESULTS-01 satisfied end-to-end. Phase 1 is COMPLETE.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM ← User input | Tip percent input (numeric only; validated via regex). All other state is read-only on Step 5. |
| Browser ← In-memory state (Zustand) | Read-only consumption in ResultsStep; no mutations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 | Tampering | Custom tip Input field accepting arbitrary characters | mitigate | Validate via regex `/^\d+(\.\d{1,2})?$/` AND `parseFloat(v) ≤ 999` before calling `setTipPercent`. Reject silently (do not update store) on invalid input. Cap `maxLength={6}` on the Input. |
| T-01-17 | Denial of Service | `computePersonTotals` called on every render of ResultsStep | mitigate | Function is O(items × people) where both are bounded by realistic restaurant-bill sizes (≤ 30 items, ≤ 12 people). Re-computation per render is well under 1ms. No memoization needed. |
| T-01-18 | Information Disclosure | Per-person totals rendered into client DOM | accept | Phase 1 single-device — no multi-user disclosure path. Phase 4 sharing will revisit. |
| T-01-19 | Repudiation | Tip changes silently update results without history | accept | Single-user wizard — no audit log requirement. User sees the live tip line update as immediate feedback. |
| T-01-20 | Tampering | Largest-remainder math could be tampered with via DevTools store mutation | accept | DevTools-level tampering by the device owner is not a threat model for a single-device tool. Browser console access by an attacker requires the attacker to already be the user. |
</threat_model>

<verification>
After both tasks (and the entire phase):
1. `npm run test -- --run` reports ≥ 84 passing tests, 0 failing
2. `npx tsc --noEmit` exits 0
3. `npm run build` succeeds (production build)
4. `npm run dev` boots; full end-to-end manual walkthrough succeeds:
   - Add Alice + Bob + Carol (Step 1, "Continue to items")
   - Add Pizza $10.00, Coke $2.50 (Step 2, "Assign items")
   - Pizza shared by Alice+Bob; Coke solo Carol (Step 3, "Set tip")
   - Tip 20% (Step 4, live tip shows "$2.50", "See results")
   - Step 5 shows: Alice $5.84, Bob $5.83, Carol $3.33; bottom strip "Total bill: $15.00"
   - Tap Alice → expands showing Pizza $5.00, Tip $0.84, Total $5.84
   - Tap Bob → Alice collapses, Bob expands
   - Back → Step 4, change tip to 0% → Step 5 totals update (Alice $5.00, Bob $5.00, Carol $2.50, Total $12.50)
5. All 6 phase requirements satisfied (PEOPLE-01, ITEMS-01, ITEMS-02, ITEMS-03, TIP-01, RESULTS-01)
</verification>

<success_criteria>
- TIP-01 and RESULTS-01 addressed end-to-end
- Phase 1 wizard fully functional from Step 1 through Step 5
- Cents-conservation invariant proven by tests AND visible in the UI
- All commits scoped with `(01-03)` prefix
- Full test suite green; production build succeeds
- Walking Skeleton from Plan 01 grew into a complete vertical product without re-architecting
</success_criteria>

<output>
After completion, create `.planning/phases/01-manual-bill-splitter/01-03-SUMMARY.md` documenting:
- SetTipStep + ResultsStep implementations
- D-01, D-02, D-03 enforcement points in the code
- Test counts (Tip 12 + Results 11 = 23 new tests this plan)
- Phase 1 closing checklist (all 6 requirements verified)
- Hand-off notes for Phase 2 (OCR will pre-populate items into the same store via `addItem` calls; no schema changes needed)
</output>
