---
phase: 08-flat-model-schema-api-surgery
plan: 05
type: execute
wave: 4
depends_on: ["08-01", "08-02", "08-03", "08-04"]
files_modified:
  - __tests__/disputeRoute.test.ts
  - __tests__/editRequestRoute.test.ts
  - __tests__/resolveDisputeRoute.test.ts
  - __tests__/resolveEditRoute.test.ts
  - __tests__/HostPanel.test.tsx
  - __tests__/ReviewHostAssignedScreen.test.tsx
  - __tests__/EditRequestForm.test.tsx
  - __tests__/editRoute.test.ts
  - __tests__/sessionRoute.test.ts
  - __tests__/sessionGetRoute.test.ts
  - __tests__/sessionClaimRoute.test.ts
  - __tests__/CollaborativeClaimingView.test.tsx
  - __tests__/PersonSlotPicker.test.tsx
  - __tests__/PersonResultsScreen.test.tsx
  - __tests__/ShareLinkButton.test.tsx
  - __tests__/ClaimableItemCard.test.tsx
autonomous: true
requirements: [CLAIM-01, CLAIM-03]
must_haves:
  truths:
    - "Every test file for a deleted route or component is removed"
    - "Every surviving test's session fixture is flat — no hostToken, editRequests, disputes, hostPersonId, or assignedBy"
    - "sessionRoute and sessionGetRoute tests assert currencyCode is persisted and returned (default 'USD')"
    - "The full vitest suite is green except the 4 remaining known pre-existing failures, which stay distinct and unmasked"
  artifacts:
    - path: "__tests__/editRoute.test.ts"
      provides: "Green /edit contract coverage (CLAIM-03, D-01, remove purges claims)"
      contains: "edit_price"
  key_links:
    - from: "__tests__ fixtures"
      to: "flat SessionPayload"
      via: "baseSession with currencyCode and no host fields"
      pattern: "currencyCode"
---

<objective>
Migrate the test suite to the flat model: delete the seven obsolete test files (routes/components removed), update the surviving fixtures and assertions to the flat schema, add currencyCode assertions, and confirm the full suite is green except the four remaining known pre-existing failures.

Purpose: Success Criterion #6 — every deleted test file has a flat-model replacement and CI is green. The CollaborativeClaimingView test, itself rewritten this phase, must pass (it is also one of the historical 5 failures — its replacement clears that one, leaving 4).
Output: A flat-model test suite; CI green modulo the 4 remaining known pre-existing failures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-flat-model-schema-api-surgery/08-RESEARCH.md
@.planning/phases/08-flat-model-schema-api-surgery/08-VALIDATION.md
@.planning/phases/08-flat-model-schema-api-surgery/08-CONTEXT.md

<interfaces>
<!-- Flat fixture target. -->
baseSession (flat): { people, items, claims: { items, personSlots, donePeople }, tips, currencyCode, createdAt }
ClaimEntry: { qty }   (no assignedBy/accepted)
POST /api/session response: { sessionId }
POST /api/session/[id]/edit: op-discriminated, immediate
</interfaces>

<known_pre_existing_failures>
<!-- Track DISTINCTLY. Do NOT fix or mask these (except CollaborativeClaimingView, rewritten this phase). -->
- __tests__/PersonSlotPicker.test.tsx — Test 2: opacity-50 vs opacity-40
- __tests__/AddPeopleStep.test.tsx — "disables CTA" + "enables CTA": button text mismatch (x2)
- __tests__/AddItemsStep.test.tsx — "tapping Continue": setStep(2) vs expected 3
- __tests__/CollaborativeClaimingView.test.tsx — Test 18: "You're all set" — REWRITTEN this phase; replacement MUST pass
</known_pre_existing_failures>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete obsolete test files and update route-test fixtures + currencyCode assertions</name>
  <files>__tests__/disputeRoute.test.ts, __tests__/editRequestRoute.test.ts, __tests__/resolveDisputeRoute.test.ts, __tests__/resolveEditRoute.test.ts, __tests__/HostPanel.test.tsx, __tests__/ReviewHostAssignedScreen.test.tsx, __tests__/EditRequestForm.test.tsx, __tests__/sessionRoute.test.ts, __tests__/sessionGetRoute.test.ts, __tests__/sessionClaimRoute.test.ts</files>
  <read_first>
    - .planning/phases/08-flat-model-schema-api-surgery/08-RESEARCH.md (Test Migration: DELETE / UPDATE tables, Pitfall 5)
    - __tests__/sessionRoute.test.ts, __tests__/sessionGetRoute.test.ts, __tests__/sessionClaimRoute.test.ts (baseSession fixtures + host-case tests)
  </read_first>
  <action>
    Delete the seven obsolete test files whose routes/components no longer exist: __tests__/disputeRoute.test.ts, __tests__/editRequestRoute.test.ts, __tests__/resolveDisputeRoute.test.ts, __tests__/resolveEditRoute.test.ts, __tests__/HostPanel.test.tsx, __tests__/ReviewHostAssignedScreen.test.tsx, and __tests__/EditRequestForm.test.tsx (the last because its component was deleted in Plan 03 — not in the research's 6-file list but required by the same logic). Then update route-test fixtures: in sessionRoute.test.ts strip hostToken/editRequests/disputes from baseSession, drop the assertion that the response carries hostToken (response is now `{ sessionId }`), assert the stored payload does NOT contain hostToken/editRequests/disputes and DOES contain currencyCode, and add a test that a POST body carrying currencyCode persists it while an absent/invalid code defaults to 'USD' (D-04). In sessionGetRoute.test.ts strip host fields from baseSession, remove the "hostToken stripped" assertion (no longer stored), and add an assertion that GET returns currencyCode. In sessionClaimRoute.test.ts strip host fields from baseSession and remove/rewrite the host-assignment tests (assignedBy:'host', assignedBy:'admin') as flat self-claims — the route no longer accepts assignedBy. Per Pitfall 5, prefer separable commits (deletions, then surviving-fixture updates) if committing incrementally.
  </action>
  <verify>
    <automated>for f in disputeRoute editRequestRoute resolveDisputeRoute resolveEditRoute; do test ! -f "__tests__/$f.test.ts" || exit 1; done; test ! -f __tests__/HostPanel.test.tsx && test ! -f __tests__/ReviewHostAssignedScreen.test.tsx && test ! -f __tests__/EditRequestForm.test.tsx && grep -q currencyCode __tests__/sessionRoute.test.ts && grep -q currencyCode __tests__/sessionGetRoute.test.ts && npx vitest run __tests__/sessionRoute.test.ts __tests__/sessionGetRoute.test.ts __tests__/sessionClaimRoute.test.ts __tests__/editRoute.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The 7 obsolete test files do not exist
    - sessionRoute.test.ts asserts currencyCode persisted + 'USD' default; no hostToken assertion remains
    - sessionGetRoute.test.ts asserts currencyCode returned; no "hostToken stripped" assertion
    - sessionClaimRoute.test.ts has no assignedBy:'host'/'admin' cases; baseSession is flat
    - `npx vitest run` over the four route test files exits 0
  </acceptance_criteria>
  <done>Obsolete route/component tests removed; route fixtures flat; currencyCode persistence + default asserted (D-04).</done>
</task>

<task type="auto">
  <name>Task 2: Update component/view test fixtures and confirm full-suite green modulo known failures</name>
  <files>__tests__/CollaborativeClaimingView.test.tsx, __tests__/PersonSlotPicker.test.tsx, __tests__/PersonResultsScreen.test.tsx, __tests__/ShareLinkButton.test.tsx, __tests__/ClaimableItemCard.test.tsx</files>
  <read_first>
    - .planning/phases/08-flat-model-schema-api-surgery/08-RESEARCH.md (Test Migration UPDATE table, Pre-existing failures list)
    - .planning/phases/08-flat-model-schema-api-surgery/08-VALIDATION.md (Known Pre-Existing Failures — track distinctly)
    - __tests__/CollaborativeClaimingView.test.tsx, __tests__/PersonSlotPicker.test.tsx, __tests__/PersonResultsScreen.test.tsx, __tests__/ShareLinkButton.test.tsx, __tests__/ClaimableItemCard.test.tsx
  </read_first>
  <action>
    Update component/view test fixtures to the flat shape. In CollaborativeClaimingView.test.tsx: remove hostToken/editRequests/disputes/hostPersonId from fixtures; delete host-badge/FAB/HostPanel/review tests; rewrite handleInlineSubmit assertions to expect calls to `/api/session/[id]/edit` (not `/edit-request`); add coverage that a delete triggers a confirm before the `op:'remove'` POST and that the confirm copy names the claimant count when the item has claims (D-02); ensure the previously-failing "You're all set" Test 18 path is corrected so the replacement passes. In PersonSlotPicker.test.tsx, PersonResultsScreen.test.tsx, ShareLinkButton.test.tsx, ClaimableItemCard.test.tsx: strip hostToken/editRequests/disputes/hostPersonId and any assignedBy:'host' claim fixtures, and remove "Assigned by host" label assertions. In ShareLinkButton.test.tsx additionally: remove hostToken from the mocked create response, remove the assertion that setHostToken was called, remove the `#hostToken=` redirect test, and add an assertion that the POST body includes currencyCode (D-04). Do NOT touch or "fix" the 4 remaining known pre-existing failures (PersonSlotPicker Test 2 opacity, AddPeopleStep x2, AddItemsStep) — leave them failing and distinct; only the rewritten CollaborativeClaimingView replacement must pass. Run the full suite and confirm the only red tests are those 4 known pre-existing failures.
  </action>
  <verify>
    <automated>! grep -REq "hostToken|editRequests|disputes|hostPersonId|Assigned by host|/edit-request" __tests__/CollaborativeClaimingView.test.tsx __tests__/PersonSlotPicker.test.tsx __tests__/PersonResultsScreen.test.tsx __tests__/ShareLinkButton.test.tsx __tests__/ClaimableItemCard.test.tsx && grep -q currencyCode __tests__/ShareLinkButton.test.tsx && npx vitest run 2>&1 | tee /tmp/p08_suite.txt; grep -Eq "PersonSlotPicker|AddPeopleStep|AddItemsStep" /tmp/p08_suite.txt; echo "Review failing list: must be ONLY the 4 known pre-existing failures"</automated>
  </verify>
  <acceptance_criteria>
    - No host concept (hostToken/editRequests/disputes/hostPersonId/Assigned by host) and no `/edit-request` reference in the five updated test files
    - ShareLinkButton.test.tsx asserts currencyCode in the POST body and no longer asserts setHostToken / `#hostToken=`
    - The rewritten CollaborativeClaimingView test passes (including the prior Test 18 path)
    - `npx vitest run` red set equals exactly the 4 remaining known pre-existing failures (PersonSlotPicker Test 2, AddPeopleStep x2, AddItemsStep) — no new failures, none of the 4 masked/fixed
  </acceptance_criteria>
  <done>All surviving fixtures flat; CollaborativeClaimingView replacement green; suite green except the 4 documented pre-existing failures (Success Criterion #6).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → route handlers | Tests assert the flat contract; no trust boundary crossed at runtime. Security-relevant only insofar as tests pin the validation/error-shape mitigations from Plan 02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Tampering (regression) | masked pre-existing failures | mitigate | Track the 4 remaining known failures distinctly; do NOT fix or mask them. CI red-set must equal exactly those 4 so a genuinely new regression cannot hide among them. |
| T-08-15 | Information Disclosure (test coverage) | error-shape assertions | mitigate | editRoute/sessionRoute tests assert generic error bodies ('Edit failed', 'Session creation failed') so the no-internals-leak mitigation (T-08-04) stays enforced by CI. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan. No slopcheck checkpoint required. |
</threat_model>

<verification>
- 7 obsolete test files deleted
- No host concepts in any surviving test fixture
- currencyCode persistence + default + GET return asserted (D-04)
- editRoute.test.ts green (CLAIM-03 + D-01 + remove-purges-claims)
- `npx vitest run` red-set == the 4 documented pre-existing failures only
</verification>

<success_criteria>
- Every deleted test file has a flat-model replacement and the suite is green modulo documented pre-existing failures (Success Criterion #6)
- CLAIM-01 / CLAIM-03 behaviors are covered by green tests
- The 4 remaining pre-existing failures stay distinct and unmasked
</success_criteria>

<output>
Create `.planning/phases/08-flat-model-schema-api-surgery/08-05-SUMMARY.md` when done
</output>
