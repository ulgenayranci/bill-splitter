---
phase: 08-flat-model-schema-api-surgery
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - stores/useBillStore.ts
  - components/wizard/ShareLinkButton.tsx
  - components/split/ClaimableItemCard.tsx
  - components/split/PersonSlotPicker.tsx
  - components/split/HostPanel.tsx
  - components/split/ReviewHostAssignedScreen.tsx
  - components/split/EditRequestForm.tsx
autonomous: true
requirements: [CLAIM-01, CLAIM-03]
must_haves:
  truths:
    - "useBillStore has no hostToken field, setHostToken action, or hostToken partialize entry"
    - "ShareLinkButton sends currencyCode in the POST /api/session body and redirects all participants to /split/[id] with no #hostToken fragment"
    - "ClaimableItemCard renders no 'Assigned by host' label and has no isHostAssigned branch"
    - "PersonSlotPicker imports SessionPayload (alias still resolves)"
    - "HostPanel, ReviewHostAssignedScreen, and EditRequestForm components no longer exist"
  artifacts:
    - path: "components/wizard/ShareLinkButton.tsx"
      provides: "currencyCode in session-create body; fragment-free redirect"
      contains: "currencyCode"
  key_links:
    - from: "components/wizard/ShareLinkButton.tsx"
      to: "POST /api/session"
      via: "fetch body with currencyCode from useBillStore"
      pattern: "currencyCode"
---

<objective>
Update the small client consumers broken by the flat schema (Plan 01) and the new create response (Plan 02), and delete the three host-only components. Wire `currencyCode` from the store into the session-create request (D-04) and remove the host-fragment redirect so every participant lands on the same URL.

Purpose: Clears the bulk of the TypeScript cascade so the large CollaborativeClaimingView refactor (Plan 04) compiles against deleted components and a host-free store. Co-dependent with Plan 02 Task 2 (Pitfall 6: the create response dropped hostToken, so ShareLinkButton must stop reading it).
Output: A host-free store, components, and a currencyCode-sending ShareLinkButton; host components deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-flat-model-schema-api-surgery/08-RESEARCH.md
@.planning/phases/08-flat-model-schema-api-surgery/08-CONTEXT.md

<interfaces>
<!-- Flat schema (Plan 01) + new create response (Plan 02). -->
SessionPayload: { people, items, claims, tips, currencyCode, createdAt }
PublicSessionPayload = SessionPayload  (alias retained — imports still resolve)
POST /api/session response: { sessionId }   (hostToken removed)
useBillStore exposes currencyCode (already stored from OCR) via useBillStore.getState()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip hostToken from useBillStore and delete the three host components</name>
  <files>stores/useBillStore.ts, components/split/HostPanel.tsx, components/split/ReviewHostAssignedScreen.tsx, components/split/EditRequestForm.tsx</files>
  <read_first>
    - stores/useBillStore.ts (BillState interface, INITIAL_STATE, setHostToken action, partialize)
    - components/split/HostPanel.tsx, components/split/ReviewHostAssignedScreen.tsx, components/split/EditRequestForm.tsx (confirm they are host-only before deleting)
    - .planning/phases/08-flat-model-schema-api-surgery/08-RESEARCH.md (Removal Map sections 3 & "stores/useBillStore.ts")
  </read_first>
  <action>
    In stores/useBillStore.ts: remove `hostToken: string | null` from the BillState interface, `hostToken: null` from INITIAL_STATE, the `setHostToken` action, and the `hostToken: s.hostToken` entry from partialize. Leave the existing `currencyCode` field/action untouched (it already exists and is consumed in Task 2). Delete the three host-only component files entirely: components/split/HostPanel.tsx, components/split/ReviewHostAssignedScreen.tsx, components/split/EditRequestForm.tsx (use `rm`). These are deleted now so Plan 04's CollaborativeClaimingView refactor compiles with their imports removed.
  </action>
  <verify>
    <automated>! grep -Eq "hostToken|setHostToken" stores/useBillStore.ts && test ! -f components/split/HostPanel.tsx && test ! -f components/split/ReviewHostAssignedScreen.tsx && test ! -f components/split/EditRequestForm.tsx && echo STORE_AND_COMPONENTS_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E 'hostToken|setHostToken' stores/useBillStore.ts` returns nothing
    - `currencyCode` still present in stores/useBillStore.ts (`grep -q currencyCode`)
    - HostPanel.tsx, ReviewHostAssignedScreen.tsx, EditRequestForm.tsx do not exist
  </acceptance_criteria>
  <done>Store carries no host token; the three host components are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Wire currencyCode into ShareLinkButton and remove the host redirect</name>
  <files>components/wizard/ShareLinkButton.tsx</files>
  <read_first>
    - components/wizard/ShareLinkButton.tsx (PendingSession interface, useBillStore selectors, res.json destructure, router.push redirect)
    - .planning/phases/08-flat-model-schema-api-surgery/08-RESEARCH.md (currencyCode Plumbing, Pitfall 3, Pitfall 6, Removal Map "ShareLinkButton.tsx")
    - .planning/phases/08-flat-model-schema-api-surgery/08-CONTEXT.md (D-04)
  </read_first>
  <action>
    In components/wizard/ShareLinkButton.tsx: read `currencyCode` from the store alongside people/items/assignments (e.g. `const { people, items, assignments, currencyCode } = useBillStore.getState()`) and include `currencyCode` in the JSON POST body to /api/session (D-04). Remove `hostToken` from the PendingSession interface; remove the `setHostToken` selector and its call; remove `hostToken` from the `res.json()` destructure (the create response is now `{ sessionId }` only — Pitfall 6); remove `hostToken` from the pendingSession object. Remove the `#hostToken=${hostToken}` fragment from `router.push()` so the redirect target is `/split/${sessionId}` (Pitfall 3 — every participant, including the creator, gets the same fragment-free URL).
  </action>
  <verify>
    <automated>grep -q "currencyCode" components/wizard/ShareLinkButton.tsx && ! grep -Eq "hostToken|setHostToken|#hostToken" components/wizard/ShareLinkButton.tsx && echo SHARELINK_FLAT</automated>
  </verify>
  <acceptance_criteria>
    - ShareLinkButton's POST body includes `currencyCode` (asserted by Plan 05 ShareLinkButton test)
    - `grep -E 'hostToken|setHostToken|#hostToken'` over ShareLinkButton.tsx returns nothing
    - `router.push` targets `/split/${sessionId}` with no hash fragment
  </acceptance_criteria>
  <done>ShareLinkButton sends currencyCode and redirects all participants to a fragment-free /split URL.</done>
</task>

<task type="auto">
  <name>Task 3: Remove host-assignment UI from ClaimableItemCard and PersonSlotPicker</name>
  <files>components/split/ClaimableItemCard.tsx, components/split/PersonSlotPicker.tsx</files>
  <read_first>
    - components/split/ClaimableItemCard.tsx (isHostAssigned memo, "Assigned by host" label, border-amber cardClasses branch)
    - components/split/PersonSlotPicker.tsx (PublicSessionPayload import)
    - .planning/phases/08-flat-model-schema-api-surgery/08-RESEARCH.md (Removal Map "ClaimableItemCard.tsx" and "PersonSlotPicker.tsx", Pitfall 2)
  </read_first>
  <action>
    In components/split/ClaimableItemCard.tsx: remove the `const isHostAssigned = myEntry?.assignedBy === 'host'` line (the flat ClaimEntry has no assignedBy), the "Assigned by host" label block, and the `isHostAssigned ? 'border-amber-200' : ''` branch in cardClasses. Keep the `ClaimEntry` type import — the prop type still uses `Record<PersonId, ClaimEntry>`, now with the simplified `{ qty }` shape. In components/split/PersonSlotPicker.tsx: since PublicSessionPayload is now an alias of SessionPayload (Plan 01, Pitfall 2), the existing import still resolves — verify it compiles; if the import path or name needs adjusting, switch it to `SessionPayload`. Make no behavioral changes beyond removing the host-assignment surface.
  </action>
  <verify>
    <automated>! grep -Eq "isHostAssigned|Assigned by host|assignedBy" components/split/ClaimableItemCard.tsx && npx tsc --noEmit 2>&1 | grep -E "ClaimableItemCard|PersonSlotPicker" | grep -v "test" || echo CARD_AND_PICKER_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E 'isHostAssigned|Assigned by host|assignedBy'` over ClaimableItemCard.tsx returns nothing
    - `npx tsc --noEmit` reports no errors in ClaimableItemCard.tsx or PersonSlotPicker.tsx (non-test)
    - ClaimEntry import retained in ClaimableItemCard.tsx
  </acceptance_criteria>
  <done>No host-assignment label or branch remains; PersonSlotPicker type import resolves under the flat schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ShareLinkButton → POST /api/session | Client supplies people/items/currencyCode at creation; server validates (Plan 02). |
| participant device → /split/[id] URL | All participants now share one fragment-free URL; no host secret travels in the URL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Information Disclosure | host-fragment URL removal | mitigate | Removing `#hostToken=` from the redirect eliminates a capability token from the URL/history entirely — strictly reduces disclosure surface. All participants get an equal, secret-free link (intended, CLAIM-01). |
| T-08-10 | Tampering | currencyCode sent from client | accept | The store value originates from OCR; the server re-validates `/^[A-Z]{3}$/` and defaults 'USD' (Plan 02 T-08-08). A malformed client value cannot persist. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan. No slopcheck checkpoint required. |
</threat_model>

<verification>
- `npx tsc --noEmit` shows no host-type errors in store/components touched here
- ShareLinkButton POST body includes currencyCode; no hostToken/fragment anywhere
- Three host components deleted
- `npx vitest run` shows no NEW failures attributable to these files (test fixtures updated in Plan 05)
</verification>

<success_criteria>
- Client no longer carries or transmits a hostToken (supports CLAIM-01)
- currencyCode reaches the session-create request (Success Criterion #5, D-04)
- Host-assignment UI removed; host components deleted (supports Success Criterion #3)
</success_criteria>

<output>
Create `.planning/phases/08-flat-model-schema-api-surgery/08-03-SUMMARY.md` when done
</output>
