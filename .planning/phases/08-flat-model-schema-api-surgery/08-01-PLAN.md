---
phase: 08-flat-model-schema-api-surgery
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/sessionSchema.ts
  - __tests__/editRoute.test.ts
autonomous: true
requirements: [CLAIM-01, CLAIM-03]
must_haves:
  truths:
    - "The session schema declares no host-role symbols (hostToken, hostPersonId, editRequests, disputes, EditRequest, EditPayload, Dispute)"
    - "SessionPayload carries a currencyCode: string field"
    - "ClaimEntry has no assignedBy or accepted field — every claim is a self-claim"
    - "A failing-by-design editRoute test exists that pins the 5 /edit operations, D-01 claim preservation, and remove-purges-claims"
  artifacts:
    - path: "lib/sessionSchema.ts"
      provides: "Flat SessionPayload + ClaimEntry + SessionClaims with currencyCode"
      contains: "currencyCode"
    - path: "__tests__/editRoute.test.ts"
      provides: "Wave-0 behavior contract for POST /api/session/[sessionId]/edit"
      contains: "edit_price"
  key_links:
    - from: "lib/sessionSchema.ts"
      to: "all SessionPayload consumers"
      via: "type import"
      pattern: "SessionPayload"
---

<objective>
Flatten `lib/sessionSchema.ts` to remove every host-role concept and add the `currencyCode` field (D-04), then author the Wave-0 failing contract test `__tests__/editRoute.test.ts` that pins the behavior of the not-yet-created `/edit` route.

Purpose: TypeScript's compiler cascade is the surgical guide for the rest of the phase — changing the schema first surfaces every broken callsite automatically (Safe Removal Order, Wave A). The Wave-0 test fixes the `/edit` route contract before it is implemented in Plan 02, sharpening CLAIM-03 and D-01.
Output: A flat schema with no host symbols + currencyCode, and a red-by-design editRoute test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-flat-model-schema-api-surgery/08-CONTEXT.md
@.planning/phases/08-flat-model-schema-api-surgery/08-RESEARCH.md
@.planning/phases/08-flat-model-schema-api-surgery/08-VALIDATION.md

<interfaces>
<!-- Current shape of lib/sessionSchema.ts BEFORE surgery. Executor edits this file. -->
ClaimEntry: { qty: number; assignedBy: 'self' | 'host'; accepted?: boolean }
SessionClaims: { items: Record<ItemId, Record<PersonId, ClaimEntry>>; personSlots: Record<PersonId, boolean>; donePeople: Record<PersonId, boolean> }
SessionPayload: { people, items, claims, hostToken, hostPersonId?, tips, editRequests, disputes, createdAt }
PublicSessionPayload = Omit<SessionPayload, 'hostToken'>
EditPayload, EditRequest, Dispute — host approval/dispute model (whole exports removed)

<!-- /edit route contract the Wave-0 test must encode (from 08-RESEARCH "Direct /edit Route Shape"). -->
POST /api/session/[sessionId]/edit body discriminated by `op`:
  { op: 'add', name: string, priceCents: number, quantity: number }
  { op: 'edit_name', itemId: string, newName: string }
  { op: 'edit_price', itemId: string, newPriceCents: number }
  { op: 'edit_quantity', itemId: string, newQuantity: number }
  { op: 'remove', itemId: string }
Success → 200 { ok: true }; not found → 404 { error: 'session_not_found' }; bad input → 400 { error }; server error → 500 { error: 'Edit failed' }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flatten sessionSchema.ts and add currencyCode</name>
  <files>lib/sessionSchema.ts</files>
  <read_first>
    - lib/sessionSchema.ts (the file being edited — full current shape)
    - .planning/phases/08-flat-model-schema-api-surgery/08-RESEARCH.md (Removal Map section 1, Pitfall 2)
    - .planning/phases/08-flat-model-schema-api-surgery/08-CONTEXT.md (D-03, D-04)
  </read_first>
  <behavior>
    - lib/sessionSchema.ts exports SessionPayload with currencyCode: string and WITHOUT hostToken, hostPersonId, editRequests, disputes
    - ClaimEntry shape is { qty: number } only — no assignedBy, no accepted
    - EditPayload, EditRequest, Dispute exports are gone
    - PublicSessionPayload remains importable as a type alias equal to SessionPayload (zero-break path per Pitfall 2)
  </behavior>
  <action>
    Edit lib/sessionSchema.ts per Removal Map section 1. From ClaimEntry remove `assignedBy` and `accepted`, leaving `{ qty: number }`. Delete the EditPayload, EditRequest, and Dispute exports entirely. From SessionPayload remove `hostToken`, `hostPersonId`, `editRequests`, and `disputes` (including their JSDoc blocks). Add `currencyCode: string` after `createdAt` with a JSDoc note: "ISO 4217 currency code. Defaults to 'USD' at creation. Display threading is Phase 10." Per D-03 do NOT add any migrateSession normalizer, v1-compat, or old-format handling. To avoid phantom-consumer breakage (Pitfall 2), keep the PublicSessionPayload export but redefine it as `export type PublicSessionPayload = SessionPayload`. Do not touch any other file — let the TypeScript cascade in later plans enumerate breakages.
  </action>
  <verify>
    <automated>grep -Eq 'currencyCode' lib/sessionSchema.ts && grep -vE '^\s*(//|\*|/\*)' lib/sessionSchema.ts | grep -Eqv 'hostToken|hostPersonId|editRequests|disputes|EditRequest|EditPayload|interface Dispute|assignedBy|accepted\?' && echo SCHEMA_FLAT</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'currencyCode: string' lib/sessionSchema.ts` returns a match
    - Stripping comment lines, `grep -E 'hostToken|hostPersonId|editRequests|disputes|EditRequest|EditPayload|Dispute|assignedBy|accepted'` over lib/sessionSchema.ts returns nothing
    - `export type PublicSessionPayload = SessionPayload` is present
    - No migrateSession / normalize / v1 token appears in the file
  </acceptance_criteria>
  <done>SessionPayload is flat with currencyCode; ClaimEntry is { qty }; host approval/dispute types deleted; PublicSessionPayload aliased to SessionPayload.</done>
</task>

<task type="auto">
  <name>Task 2: Author Wave-0 failing editRoute contract test</name>
  <files>__tests__/editRoute.test.ts</files>
  <read_first>
    - .planning/phases/08-flat-model-schema-api-surgery/08-RESEARCH.md (Direct /edit Route Shape, Pitfall 4)
    - .planning/phases/08-flat-model-schema-api-surgery/08-VALIDATION.md (Wave 0 Requirements)
    - __tests__/sessionClaimRoute.test.ts (existing route-test harness: how redis is mocked, how POST handler + params Promise are invoked)
    - app/api/session/[sessionId]/edit-request/route.ts (validation shape to port)
    - app/api/session/[sessionId]/resolve-edit/route.ts (mutation shape to port)
  </read_first>
  <action>
    Create __tests__/editRoute.test.ts mirroring the existing route-test harness in sessionClaimRoute.test.ts (same `redis` mock from @/lib/redis, same pattern of importing POST and invoking it with a Request + a params Promise resolving { sessionId }). Import POST from `@/app/api/session/[sessionId]/edit/route` — this import will fail because the route does not exist yet; that is the intended RED state. Write assertions for each `op`: `add` appends an item (id via nanoid) and returns 200 { ok: true }; `edit_name` updates item.name in place; `edit_price` updates item.priceCents AND a claim entry that existed on that item before the edit still exists after (D-01 — claims preserved, not dropped); `edit_quantity` updates quantity, and reducing newQuantity below current totalClaimed returns 400 (Pitfall 4); `remove` deletes the item from items[] AND purges claims.items[itemId]. Add 404 (session_not_found) and 400 (invalid op / missing itemId) cases. Use a flat baseSession fixture with currencyCode and NO host fields. Do NOT implement the route — only the test.
  </action>
  <verify>
    <automated>test -f __tests__/editRoute.test.ts && ! npx vitest run __tests__/editRoute.test.ts >/dev/null 2>&1 && echo RED_AS_EXPECTED</automated>
  </verify>
  <acceptance_criteria>
    - `__tests__/editRoute.test.ts` exists and imports POST from the edit route path
    - `npx vitest run __tests__/editRoute.test.ts` exits non-zero (route not implemented — RED by design)
    - Test bodies assert all 5 ops + D-01 claim preservation on edit_price + remove-purges-claims + edit_quantity-below-claimed rejection + 404/400 paths
    - baseSession fixture contains currencyCode and contains no hostToken/editRequests/disputes
  </acceptance_criteria>
  <done>A red-by-design editRoute test pins the /edit contract; it goes green in Plan 02 (route) and is finalized in Plan 04 (test migration).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/session/[id]/edit | Untrusted item-mutation input crosses here once the route exists (Plan 02). The schema/test in this plan define the validated shape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Elevation of Privilege | Removal of hostToken/hostPersonId from SessionPayload | accept | Removing host gating is intentional (CLAIM-01/03) — any participant may now mutate. No NEW exposure: the schema carries no secret to leak. The old hostToken was the only auth secret; its removal shrinks the attack surface. |
| T-08-02 | Tampering | currencyCode field added to SessionPayload | mitigate | The field is a plain ISO-4217 string; Plan 02's POST handler must validate `/^[A-Z]{3}$/` and default 'USD'. This plan only declares the type — no untrusted write path is opened here. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan (RESEARCH: "no new npm dependencies"). No slopcheck checkpoint required. |
</threat_model>

<verification>
- `grep -c currencyCode lib/sessionSchema.ts` ≥ 1
- Host symbols absent from non-comment lines of lib/sessionSchema.ts
- `npx vitest run __tests__/editRoute.test.ts` is RED (route not yet built)
- No new packages added (`git diff --stat package.json` is empty)
</verification>

<success_criteria>
- SessionPayload is flat (no host symbols) and carries currencyCode (Success Criterion #5)
- editRoute contract test exists and is red-by-design, encoding CLAIM-03 + D-01 + remove-purges-claims
- No migrateSession normalizer introduced (D-03)
</success_criteria>

<output>
Create `.planning/phases/08-flat-model-schema-api-surgery/08-01-SUMMARY.md` when done
</output>
