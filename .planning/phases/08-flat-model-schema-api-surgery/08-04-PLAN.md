---
phase: 08-flat-model-schema-api-surgery
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - app/split/[sessionId]/CollaborativeClaimingView.tsx
autonomous: true
requirements: [CLAIM-01, CLAIM-03]
must_haves:
  truths:
    - "CollaborativeClaimingView contains no host token, isHost memo, HostPanel/ReviewHostAssignedScreen render, host FAB, or pending-approval UI"
    - "Item add/edit actions POST to /api/session/[id]/edit (not /edit-request) and apply immediately"
    - "An item delete shows a client-side confirm before POSTing op:'remove'; when the item has claims the confirm names the claimant count (D-02)"
    - "derivePhase no longer has a host-assignment/review branch: donePeople → tip → results"
  artifacts:
    - path: "app/split/[sessionId]/CollaborativeClaimingView.tsx"
      provides: "Flat collaborative claiming view wired to /edit with D-02 delete confirm"
      contains: "/edit"
  key_links:
    - from: "app/split/[sessionId]/CollaborativeClaimingView.tsx"
      to: "POST /api/session/[id]/edit"
      via: "handleInlineSubmit fetch"
      pattern: "/edit"
    - from: "delete action"
      to: "op: 'remove'"
      via: "client confirm naming claimant count then fetch"
      pattern: "confirm"
---

<objective>
Refactor `app/split/[sessionId]/CollaborativeClaimingView.tsx` (the largest consumer, ~705 lines, 15+ host touchpoints) to the flat model: delete all host state/memos/UI, rewire item add/edit/remove to the new `/edit` route, add the D-02 client-side delete confirmation, and simplify the phase machine.

Purpose: This makes CLAIM-01 and CLAIM-03 true in the UI — any participant edits/removes items immediately, no host, no approval queue, no review screen. This is deletion + rewiring of existing behavior, NOT new UI design (Phase 9 redesigns the Bill View).
Output: A host-free CollaborativeClaimingView wired to /edit with a stakes-naming delete confirm.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-flat-model-schema-api-surgery/08-RESEARCH.md
@.planning/phases/08-flat-model-schema-api-surgery/08-CONTEXT.md

<interfaces>
<!-- Flat schema (Plan 01); /edit route (Plan 02); host components deleted (Plan 03). -->
SessionPayload: { people, items, claims, tips, currencyCode, createdAt }   (no editRequests/disputes/hostPersonId)
POST /api/session/[id]/edit body: { op: 'add'|'edit_name'|'edit_price'|'edit_quantity'|'remove', ...payload } → 200 { ok: true }
Deleted (imports must be removed): HostPanel, ReviewHostAssignedScreen, EditRequestForm
claimant count for an item = Object.keys(session.claims.items[itemId] ?? {}).length
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete host state, memos, and UI from CollaborativeClaimingView</name>
  <files>app/split/[sessionId]/CollaborativeClaimingView.tsx</files>
  <read_first>
    - app/split/[sessionId]/CollaborativeClaimingView.tsx (full file — host touchpoints enumerated in research)
    - .planning/phases/08-flat-model-schema-api-surgery/08-RESEARCH.md (Removal Map "CollaborativeClaimingView.tsx" — line-by-line touchpoint list, Pitfall 3)
  </read_first>
  <action>
    Remove every host touchpoint listed in the research Removal Map for this file: the `hostTokenParam` state + the useEffect reading the URL hash; the host auto-restore useEffect that reads `session.hostPersonId`; the `isHost` memo; the `pendingCount` memo references to `session.editRequests`/`session.disputes`; the `hostPanelOpen` state and the `HostPanel` render; the `ReviewHostAssignedScreen` render and the `phase === 'review'` branch; the `hasHostAssignedItems()` and `hasUnacceptedHostItems()` helpers; the `handleBackFromTip()` review-branch logic; the "Pending approval" UI blocks for items and for pending add-requests; and the host FAB button. Remove the now-unused imports of HostPanel, ReviewHostAssignedScreen, and EditRequestForm (deleted in Plan 03). Do not introduce any new UI — Phase 9 owns the Bill View redesign.
  </action>
  <verify>
    <automated>F="app/split/[sessionId]/CollaborativeClaimingView.tsx"; ! grep -Eq "hostToken|hostPersonId|isHost|HostPanel|ReviewHostAssignedScreen|EditRequestForm|editRequests|disputes|Pending approval|review" "$F" && echo VIEW_HOST_FREE</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E 'hostToken|hostPersonId|isHost|HostPanel|ReviewHostAssignedScreen|EditRequestForm|editRequests|disputes|Pending approval'` over the file returns nothing
    - No remaining `phase === 'review'` branch
    - Imports of the three deleted components are gone
  </acceptance_criteria>
  <done>The view has no host state, memos, imports, or pending-approval/review UI.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire add/edit/remove to /edit, add D-02 delete confirm, simplify phase machine</name>
  <files>app/split/[sessionId]/CollaborativeClaimingView.tsx</files>
  <read_first>
    - app/split/[sessionId]/CollaborativeClaimingView.tsx (handleInlineSubmit, handleDone, derivePhase after Task 1)
    - .planning/phases/08-flat-model-schema-api-surgery/08-RESEARCH.md (Direct /edit Route Shape "Client-side pattern" + "D-02 confirm for remove", Removal Map handleInlineSubmit/handleDone/derivePhase)
    - .planning/phases/08-flat-model-schema-api-surgery/08-CONTEXT.md (D-01, D-02 — confirm is client-side, edits apply immediately)
  </read_first>
  <action>
    In handleInlineSubmit: replace every `/api/session/${sessionId}/edit-request` fetch with a `/api/session/${sessionId}/edit` call, sending the new `op`-discriminated body (`add`, `edit_name`, `edit_price`, `edit_quantity`) — fire one op per changed field in sequence as the existing code already does, then refresh via SWR. Edits apply immediately, no confirm (CLAIM-03 / D-02). For delete: before POSTing `{ op: 'remove', itemId }`, show a client-side confirmation (D-02 — always confirm). Derive the claimant count from `Object.keys(session.claims.items[itemId] ?? {}).length`; when it is > 0, the confirm copy must name the stakes (e.g. "N people claimed this — delete anyway?"); when 0, a generic confirm. Only POST remove if the user confirms. In handleDone: remove the `hasHostAssigned` detection — on done, always transition to 'tip'. In derivePhase: remove the `hasUnacceptedHostItems` branch and simplify to donePeople → 'tip', tips confirmed → 'results'. Rely on existing share recalculation (computePersonShareFromClaims) for D-01 — no client-side share math needed when price/qty changes; SWR returns the updated payload and shares recompute at render.
  </action>
  <verify>
    <automated>F="app/split/[sessionId]/CollaborativeClaimingView.tsx"; grep -q "/edit" "$F" && ! grep -q "/edit-request" "$F" && grep -Eiq "confirm" "$F" && echo WIRED_TO_EDIT_WITH_CONFIRM</automated>
  </verify>
  <acceptance_criteria>
    - The file references `/api/session/${sessionId}/edit` and contains no `/edit-request`
    - A confirm gate exists before the `op: 'remove'` POST; the confirm copy references the claimant count when > 0 (D-02)
    - handleDone transitions to 'tip' with no host branch; derivePhase has no host/review branch
    - `npx tsc --noEmit` reports no errors in this file
  </acceptance_criteria>
  <done>Item add/edit/remove flow through /edit immediately; delete always confirms and names the stakes when claimed; phase machine is host-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| participant UI → POST /api/session/[id]/edit | Any participant fires immediate item mutations; server validates (Plan 02). |
| client confirm → op:'remove' | The D-02 confirm is a UI guard only; the server executes the delete unconditionally. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Repudiation / accidental destruction | item delete | mitigate | D-02 client-side confirm before every remove; when claimed, the prompt names the claimant count so a tap can't silently erase someone's claim. Server-side validation (Plan 02) is the authoritative guard. |
| T-08-12 | Elevation of Privilege | any participant edits/removes | accept | Intended per CLAIM-01/03 — the flat model removes host gating. No new server exposure: /edit validates inputs (Plan 02 T-08-03). |
| T-08-13 | Information Disclosure | removed pending-approval/host UI | mitigate | Deleting the host FAB, HostPanel, and pending-approval blocks removes UI that surfaced host-only capability; nothing host-specific leaks to participants. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan. No slopcheck checkpoint required. |
</threat_model>

<verification>
- File is host-free (no hostToken/isHost/HostPanel/editRequests/disputes/pending-approval/review)
- Add/edit/remove POST to /edit; delete is confirm-gated with claimant count
- `npx tsc --noEmit` clean for this file
- The rewritten CollaborativeClaimingView test (Plan 05) passes
</verification>

<success_criteria>
- Any participant can edit/remove items directly via /edit immediately (Success Criterion #2, CLAIM-03)
- Deletes always confirm, naming the stakes when claimed (D-02)
- No host approval/review path remains in the view (Success Criterion #1, CLAIM-01)
</success_criteria>

<output>
Create `.planning/phases/08-flat-model-schema-api-surgery/08-04-SUMMARY.md` when done
</output>
