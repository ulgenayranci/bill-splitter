---
phase: 10-results-screen-tip-modal-currency-display
plan: 04
type: execute
wave: 3
depends_on: ["10-01", "10-03"]
files_modified:
  - components/split/TipScreen.tsx
  - app/split/[sessionId]/CollaborativeClaimingView.tsx
  - __tests__/TipScreen.test.tsx
autonomous: true
requirements: [TIP-02, CURR-02]
must_haves:
  truths:
    - "TipScreen renders as Dialog content (no full-page <main>, no AppHeader, no sticky Back header, no fixed bottom bar)"
    - "All TipScreen tip logic (presets 10/15/20, custom %, MAX_TIP_PERCENT=100 clamp, $0 confirm enabled, POST /tip, mutate) is preserved verbatim"
    - "TipScreen amounts render with the session currencyCode via formatCents"
    - "CollaborativeClaimingView Phase union is 'claiming' | 'results' (the 'tip' phase is removed)"
    - "Tapping Done goes straight to Results (setPhase('results')); a returning donePeople user lands on Results, not a removed 'tip' phase"
    - "The Tip Dialog is mounted at top level of the results render and opened from the Results screen's 'Add a tip?' button; on confirm the dialog closes and totals update via mutate"
  artifacts:
    - path: "components/split/TipScreen.tsx"
      provides: "Dialog-content tip form with currencyCode prop; onBack removed"
      contains: "currencyCode"
    - path: "app/split/[sessionId]/CollaborativeClaimingView.tsx"
      provides: "two-phase machine (claiming|results), tipDialogOpen state, Tip Dialog mount, currencyCode threading"
      contains: "tipDialogOpen"
    - path: "__tests__/TipScreen.test.tsx"
      provides: "updated render helper (no onBack, with currencyCode) + EUR formatting case"
      contains: "currencyCode"
  key_links:
    - from: "CollaborativeClaimingView results render"
      to: "PersonResultsScreen onAddTip"
      via: "onAddTip={() => setTipDialogOpen(true)}"
      pattern: "setTipDialogOpen"
    - from: "CollaborativeClaimingView TipScreen"
      to: "session.currencyCode"
      via: "currencyCode={session.currencyCode ?? 'USD'}"
      pattern: "currencyCode"
    - from: "derivePhase"
      to: "'results'"
      via: "donePeople[personId] → results (tip phase removed)"
      pattern: "donePeople"
---

<objective>
Convert `TipScreen` from a full-page route screen into shadcn Dialog content and rewire `CollaborativeClaimingView`'s phase machine so the tip is an optional modal launched from Results (D-01) rather than a mandatory linear `tip` phase (TIP-02). All tip logic is kept verbatim; only the page chrome is removed and a `currencyCode` prop is threaded through (CURR-02). "Done" now goes straight to Results; the Tip Dialog is mounted at the top level of the results render and opened by the Results screen's "Add a tip?" button (wired to Plan 03's `onAddTip` prop). On confirm, the dialog closes and `mutate()` refreshes totals.

Purpose: Closes TIP-02 (tip via modal from Results) and the tip-side of CURR-02. Removes the linear `tip` phase per D-01.
Output: TipScreen as Dialog content; CollaborativeClaimingView two-phase machine + Tip Dialog mount; updated TipScreen tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-results-screen-tip-modal-currency-display/10-RESEARCH.md
@.planning/phases/10-results-screen-tip-modal-currency-display/10-PATTERNS.md
@.planning/phases/10-results-screen-tip-modal-currency-display/10-UI-SPEC.md

<interfaces>
<!-- NEW TipScreen props (PATTERNS.md): keep sessionId, personId, itemSubtotalCents, onTipConfirmed, mutate; ADD currencyCode?: string; REMOVE onBack. -->
export interface TipScreenProps {
  sessionId: string
  personId: string
  itemSubtotalCents: number
  currencyCode?: string
  onTipConfirmed: () => void
  mutate: () => Promise<unknown>
}

<!-- CollaborativeClaimingView current references (verified against the live file): -->
- type Phase = 'claiming' | 'tip' | 'results'   (line 53)  -> becomes 'claiming' | 'results'
- derivePhase (lines 68-74): currently returns 'results' if tips?.[personId] defined, else 'tip' if donePeople[personId], else 'claiming'
- phase state useState<Phase>('claiming') (line 82); restore path setPhase(derivePhase(...)) (line 137)
- submitDone path sets setPhase('tip') (line 348) -> becomes setPhase('results')
- personalShare = computePersonShareFromClaims(selectedPersonId, items, claims, tips?.[id] ?? 0) (lines 531-537)
- phase==='tip' render block (lines 539-550) -> REMOVE
- phase==='results' render: <PersonResultsScreen ... onBack={() => setPhase('tip')} /> (lines 552-554) -> REPLACE
- TipScreen + PersonResultsScreen already imported (lines 26-27); Dialog/DialogContent already imported (lines 9-15)
- existing unclaimed-warning Dialog uses <DialogContent showCloseButton={false}> as the mount template
- handleBackToClaiming() (lines 395-413) POSTs done:false then setPhase('claiming') -- reuse for Edit bill (onEditBill)

<!-- PersonResultsScreen NEW props from Plan 03: session, personId, currencyCode, onAddTip, onEditBill, sessionId (+ optional onCurrencyChange if Plan 03 added it). Read the final file to confirm. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update TipScreen tests for Dialog content + currency (RED)</name>
  <files>__tests__/TipScreen.test.tsx</files>
  <read_first>
    - __tests__/TipScreen.test.tsx (existing renderTip helper passing onBack; Tests 1-8 incl. Test 6 onBack and Test 7 $0-confirm)
    - components/split/TipScreen.tsx (current props + tip logic to preserve)
    - .planning/phases/10-results-screen-tip-modal-currency-display/10-PATTERNS.md (section "TipScreen.tsx" -- test-extend notes)
  </read_first>
  <behavior>
    - Render helper passes currencyCode and NO onBack
    - Existing preset/custom/clamp/$0-confirm/POST tests still pass against the Dialog-content output
    - New: currencyCode='EUR' formats tip-amount-display and tip-total-display with the euro symbol
  </behavior>
  <action>
    Update `renderTip` in `__tests__/TipScreen.test.tsx`: remove the `onBack` prop and add `currencyCode` (default 'USD' in the helper). Delete or repurpose Test 6 (the `onBack` test) -- TipScreen no longer owns a Back button (Dialog close is the parent's concern); if repurposing, assert the component no longer renders a "Back" button. Keep Tests 1-5, 7, 8 intact (presets, custom %, 100% clamp, $0 Confirm-enabled, POST /tip + mutate). Add a new case rendering with `currencyCode='EUR'` and asserting the `tip-amount-display` and `tip-total-display` testids contain the euro symbol. Because TipScreen becomes Dialog content, ensure the helper does not assert on removed chrome (AppHeader, sticky header, fixed bottom bar). Run and confirm the EUR case (and the no-onBack helper) FAIL against the current full-page TipScreen.
  </action>
  <verify>
    <automated>npx vitest run __tests__/TipScreen.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - renderTip no longer passes onBack and passes currencyCode
    - A test asserts EUR formatting (euro symbol) on the tip amount + total displays
    - No test asserts on AppHeader / sticky Back header / fixed bottom bar
    - New/updated cases FAIL against the current component
  </acceptance_criteria>
  <done>TipScreen tests describe Dialog-content shape + currency formatting and fail against the current full-page screen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert TipScreen to Dialog content with currencyCode (GREEN)</name>
  <files>components/split/TipScreen.tsx</files>
  <read_first>
    - components/split/TipScreen.tsx (full current file -- tip state, applyPreset, applyCustom, MAX_TIP_PERCENT, handleConfirm at lines 35-80 must be KEPT verbatim; full-page wrappers at lines 84-97 and 152-169 must be REMOVED)
    - .planning/phases/10-results-screen-tip-modal-currency-display/10-PATTERNS.md (section "TipScreen.tsx" -- keep/remove lists + new return shape)
    - .planning/phases/10-results-screen-tip-modal-currency-display/10-UI-SPEC.md (Screen Inventory #2 Tip Modal layout + states; Copywriting; Accessibility -- Confirm aria-label when submitting)
  </read_first>
  <action>
    Replace the props interface with the NEW TipScreenProps from the interfaces block (remove `onBack`, add `currencyCode?: string`). KEEP verbatim: `tipCents`/`customPercentText`/`submitting`/`error` state, `applyPreset`, `MAX_TIP_PERCENT = 100`, `applyCustom`, `handleConfirm` (POST /tip, mutate, onTipConfirmed, error "Couldn't save tip — try again"), and `personalTotal = itemSubtotalCents + tipCents`. REMOVE the full-page chrome: the `<main className="...min-h-screen...">` wrapper, `<AppHeader />`, the sticky `<header>` with the Back `<Button>`, and the `fixed bottom-0 ...` CTA bar wrapper. Return Dialog-friendly content only: a `flex flex-col gap-6 px-6 py-4` container with the "Add a tip?" heading (20px semibold), the tip-amount display (`text-[28px] font-semibold text-amber-600`, data-testid="tip-amount-display") with "Your tip" sublabel, the three preset Buttons (h-11), the Custom % `<Input inputMode="numeric">`, a `<Separator/>`, the "Your total" row (16px semibold, data-testid="tip-total-display"), the inline `role="alert"` error, and the "Confirm tip" Button (`h-12 w-full bg-amber-600 hover:bg-amber-700`, disabled while submitting, aria-label "Confirming tip…" when submitting else "Confirm tip", Loader2 spinner while submitting). Thread `currencyCode` into BOTH formatCents calls: `formatCents(tipCents, currencyCode)` and `formatCents(personalTotal, currencyCode)`. Drop the now-unused AppHeader import. Do NOT change the POST body or tip math.
  </action>
  <verify>
    <automated>npx vitest run __tests__/TipScreen.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - TipScreen no longer imports/renders AppHeader, has no `min-h-screen` main wrapper, no sticky Back header, no `fixed bottom-0` bar
    - TipScreenProps has currencyCode and no onBack
    - Both formatCents calls pass currencyCode; EUR test shows the euro symbol
    - applyPreset/applyCustom/MAX_TIP_PERCENT/handleConfirm/$0-confirm behavior unchanged (Tests 1-5,7,8 green)
    - `npx vitest run __tests__/TipScreen.test.tsx` exits 0; `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>TipScreen is Dialog content with currency-aware amounts; all tip logic preserved; tests + tsc green.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire CollaborativeClaimingView phase machine + mount Tip Dialog</name>
  <files>app/split/[sessionId]/CollaborativeClaimingView.tsx</files>
  <read_first>
    - app/split/[sessionId]/CollaborativeClaimingView.tsx (Phase union line 53; derivePhase 68-74; phase state line 82; restore-from-localStorage setPhase line 137; submitDone setPhase('tip') line 348; handleBackToClaiming 395-413; personalShare 531-537; phase==='tip' block 539-550; phase==='results' lines 552-554; existing unclaimed-warning Dialog mount)
    - .planning/phases/10-results-screen-tip-modal-currency-display/10-PATTERNS.md (section "CollaborativeClaimingView.tsx" -- every concrete change + Pitfall 4 top-level state rule)
    - .planning/phases/10-results-screen-tip-modal-currency-display/10-RESEARCH.md (Critical Pattern: Tip-as-Dialog Conversion + Pitfalls 3 and 4)
    - components/split/PersonResultsScreen.tsx (post Plan 03 -- confirm final props: currencyCode, onAddTip, onEditBill, sessionId, and whether an onCurrencyChange prop exists)
    - components/split/TipScreen.tsx (post Task 2 -- NEW props: currencyCode, no onBack)
  </read_first>
  <action>
    Make these concrete edits, using the existing unclaimed-warning Dialog as the structural template:
    1. Change `type Phase = 'claiming' | 'tip' | 'results'` (line 53) to `type Phase = 'claiming' | 'results'`.
    2. Update `derivePhase` (lines 68-74): return `'results'` when `session.claims?.donePeople?.[personId]` is truthy, else `'claiming'`. Drop the separate `tips?.[personId]` branch and the `'tip'` return (Pitfall 3 -- a done user with no tip must NOT route to a removed phase).
    3. In the submitDone success path, change `setPhase('tip')` (line 348) to `setPhase('results')` (D-01: Done goes straight to Results).
    4. Declare `const [tipDialogOpen, setTipDialogOpen] = useState(false)` at the TOP LEVEL of the component (alongside the existing dialog-state declarations near showUnclaimedWarning), NOT inside any phase branch (Pitfall 4).
    5. REMOVE the entire `if (phase === 'tip') { return <TipScreen ... /> }` block (lines 539-550).
    6. REPLACE the `phase === 'results'` return (lines 552-554) with a fragment that renders `<PersonResultsScreen session={session} personId={selectedPersonId} currencyCode={session.currencyCode ?? 'USD'} onAddTip={() => setTipDialogOpen(true)} onEditBill={() => void handleBackToClaiming()} sessionId={sessionId} />` (also pass `onCurrencyChange={handleCurrencyChange}` if Plan 03's final interface declares it) AND a sibling `<Dialog open={tipDialogOpen} onOpenChange={setTipDialogOpen}><DialogContent showCloseButton={false}><TipScreen sessionId={sessionId} personId={selectedPersonId} itemSubtotalCents={personalShare.itemSubtotal} currencyCode={session.currencyCode ?? 'USD'} onTipConfirmed={() => setTipDialogOpen(false)} mutate={mutate} /></DialogContent></Dialog>`.
    7. Currency write path reconciliation: read PersonResultsScreen's final props. If Plan 03 expects a parent `onCurrencyChange`, add `async function handleCurrencyChange(newCode: string) { await fetch(`/api/session/${sessionId}/edit`, { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ op:'update_currency', currencyCode:newCode }) }); await mutate() }` (mirroring the existing edit fetch+mutate pattern in this file) and pass it down. If Plan 03 performs the fetch internally and only needs the SWR poll to revalidate, this step is a no-op.
    Ensure NO lingering reference to the removed `'tip'` phase string remains anywhere in the file.
  </action>
  <verify>
    <automated>grep -n "'tip'" 'app/split/[sessionId]/CollaborativeClaimingView.tsx' || true; npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'tip'" app/split/[sessionId]/CollaborativeClaimingView.tsx` returns no phase-literal matches (Phase union is 'claiming' | 'results')
    - derivePhase returns 'results' for donePeople and 'claiming' otherwise; submitDone sets 'results'
    - tipDialogOpen is declared at component top level; the Tip Dialog and PersonResultsScreen render together in the results branch
    - PersonResultsScreen receives currencyCode, onAddTip (opens dialog), onEditBill (handleBackToClaiming), sessionId
    - `npx tsc --noEmit` exits 0; `npx vitest run` exits 0 (full suite green)
  </acceptance_criteria>
  <done>The linear tip phase is gone; Done goes to Results; the Tip Dialog opens from Results and refreshes totals on confirm; full suite + tsc green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tip Dialog → /api/session/[sessionId]/tip | participant-entered tipCents crosses to server (existing route) |
| phase machine → render | derivePhase routes a restored identity to a screen based on Redis session state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Tampering | tipCents POSTed from the Tip Dialog | mitigate | Reuses the existing /tip route whose server-side `MAX_TIP_CENTS = 100_000` cap is authoritative; client clamps custom % to MAX_TIP_PERCENT=100 (unchanged logic) |
| T-10-10 | Denial of Service | derivePhase routing a donePeople user to a removed 'tip' phase (blank screen) | mitigate | derivePhase updated to map donePeople -> 'results' (Pitfall 3); no code path returns the removed phase literal |
| T-10-11 | Tampering | Tip Dialog state reset on re-render if mounted inside a phase branch | mitigate | tipDialogOpen declared at component top level, Dialog mounted unconditionally in the results return (Pitfall 4) |
</threat_model>

<verification>
- `npx vitest run` -- full suite green (TipScreen + PersonResultsScreen + CollaborativeClaimingView + all prior)
- `npx tsc --noEmit` -- no type errors
- Grep gates: `grep -c tipDialogOpen 'app/split/[sessionId]/CollaborativeClaimingView.tsx'` >=2; `grep "'tip'" 'app/split/[sessionId]/CollaborativeClaimingView.tsx'` returns no phase-literal
</verification>

<success_criteria>
- Tip is an optional modal launched from Results; Done goes straight to Results (no linear tip phase)
- Tip Dialog confirms via existing /tip route and refreshes totals via mutate; currency threaded into tip amounts
- A returning done user lands on Results; no blank-screen route to a removed phase
- Full test suite and TypeScript both clean
</success_criteria>

<output>
Create `.planning/phases/10-results-screen-tip-modal-currency-display/10-04-SUMMARY.md` when done
</output>
