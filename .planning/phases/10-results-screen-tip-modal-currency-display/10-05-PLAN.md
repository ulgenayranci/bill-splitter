---
phase: 10-results-screen-tip-modal-currency-display
plan: "05"
type: tdd
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [CURR-01, CURR-02]
files_modified:
  - components/split/ClaimableItemCard.tsx
  - app/split/[sessionId]/CollaborativeClaimingView.tsx
  - components/split/PersonResultsScreen.tsx
  - stores/useBillStore.ts
  - __tests__/ClaimableItemCard.test.tsx
  - __tests__/PersonResultsScreen.test.tsx

must_haves:
  truths:
    - "The OCR-detected currency symbol is shown on every amount in the claiming / Bill View (ClaimableItemCard), not only on the Results screen."
    - "Each person's Results breakdown shows an explicit items-only Subtotal row and an in-card Total row, so the total reads items -> Subtotal -> Your tip -> Total."
    - "Tapping New Split (and confirming) lands on the home page on the FIRST tap and does not bounce back to the same bill."
  artifacts:
    - path: "components/split/ClaimableItemCard.tsx"
      provides: "currencyCode prop threaded into both formatCents calls"
      contains: "currencyCode"
    - path: "components/split/PersonResultsScreen.tsx"
      provides: "Subtotal + in-card Total rows in current-user breakdown; store reset on New Split"
      contains: "results-subtotal"
  key_links:
    - from: "app/split/[sessionId]/CollaborativeClaimingView.tsx"
      to: "components/split/ClaimableItemCard.tsx"
      via: "currencyCode prop at render site (:658-666)"
      pattern: "currencyCode=\\{session.currencyCode"
    - from: "components/split/PersonResultsScreen.tsx handleNewSplit"
      to: "stores/useBillStore.ts reset()"
      via: "useBillStore.getState().reset() before router.push('/')"
      pattern: "useBillStore\\.getState\\(\\)\\.reset\\(\\)"
---

<objective>
Close 3 UAT gaps from Phase 10 (10-HUMAN-UAT.md). All three are small, surgical fixes with precise root causes already diagnosed — do NOT re-investigate. Each gap is one task.

Purpose: The currency the OCR detected (e.g. Turkish Lira) must show from the first screen onward (not "corrected" only at the end — a trust issue); the Results per-person card must read legibly (items -> Subtotal -> Your tip -> Total); and New Split must work on the first tap.

Output: currencyCode threaded into ClaimableItemCard; Subtotal + in-card Total rows in the Results current-user card; persisted Zustand store cleared on New Split. Plus targeted vitest tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-results-screen-tip-modal-currency-display/10-HUMAN-UAT.md
@.planning/phases/10-results-screen-tip-modal-currency-display/10-03-SUMMARY.md

<interfaces>
<!-- Extracted from codebase. Executor: use these directly, no exploration needed. -->

formatCents (lib/billMath.ts:26) — `formatCents(cents: number, currencyCode?: string): string`
  - No currencyCode -> legacy `$X.XX`.
  - With a valid ISO 4217 code -> Intl.NumberFormat currency symbol (e.g. 'EUR' -> €, 'TRY' -> ₺/TRY).
  - Invalid/empty code -> falls back to `$X.XX`.

computePersonShareFromClaims (lib/billMath.ts) returns `{ itemSubtotal, tip, total, lineItems }`
  - itemSubtotal = items only; tip = tip cents; total = itemSubtotal + tip. ALL three already available — NO math change needed.

ClaimableItemCard (components/split/ClaimableItemCard.tsx) current props:
  `{ item, claimsForItem, myPersonId, peopleById, onQtyChange, onShareChange?, errorMessage? }`
  - formatCents calls: :126 (item.priceCents, line price) and :207 (yourShareCents, "your share:" line). Both currently call formatCents WITHOUT a currency code -> default `$`.

CollaborativeClaimingView render site (app/split/[sessionId]/CollaborativeClaimingView.tsx:658-666):
  Renders <ClaimableItemCard .../>. `session.currencyCode` is in scope here and is ALREADY passed to PersonResultsScreen and TipScreen elsewhere in this file.

useBillStore (stores/useBillStore.ts):
  - `setSessionId: (id) => set({ sessionId: id })` at :141
  - `reset: () => set((s) => { revoke blob if any; return { ...INITIAL_STATE } })` at :142-146 — clears sessionId (persisted via partialize :169). reset() ALREADY EXISTS — use it; do NOT add a new action.

PersonResultsScreen.handleNewSplit (components/split/PersonResultsScreen.tsx:113-120):
  Currently removes only `localStorage split:${sessionId}:personId`, then `router.push('/')`. It does NOT clear the persisted store sessionId, so app/page.tsx:22-26 redirects back to /split/{sessionId}.

PersonResultsScreen current-user breakdown (PersonResultsScreen.tsx):
  - line items <ul> ends ~:209
  - tip block (Separator + "Your tip" row, data-testid="results-tip") at :211-225
  - headline "Your share" = share.total (items+tip) at :172-182, data-testid="results-total"
  - NO Subtotal row and NO in-card Total row exist today.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread currencyCode through ClaimableItemCard (Gap 1 — major)</name>
  <files>components/split/ClaimableItemCard.tsx, app/split/[sessionId]/CollaborativeClaimingView.tsx, __tests__/ClaimableItemCard.test.tsx</files>
  <behavior>
    - RED: add a test to __tests__/ClaimableItemCard.test.tsx that renders ClaimableItemCard with `currencyCode="EUR"` and a single-qty unclaimed item, and asserts the rendered price text contains the € symbol (NOT '$'). Use the existing direct-JSX render style in that file (no render helper) — copy the prop set from an existing single-qty test and add `currencyCode="EUR"`. Model the assertion on PersonResultsScreen.test.tsx Test 13 (EUR -> € in amount). This test fails until the prop is wired.
  </behavior>
  <action>
    In components/split/ClaimableItemCard.tsx: add `currencyCode: string` to `ClaimableItemCardProps` (interface at :11-19), destructure it in the component signature (:23-31), and pass it as the 2nd arg to BOTH formatCents calls — `formatCents(item.priceCents, currencyCode)` at :126 and `formatCents(yourShareCents, currencyCode)` at :207. No other logic changes.
    In app/split/[sessionId]/CollaborativeClaimingView.tsx at the ClaimableItemCard render site (:658-666): add the prop `currencyCode={session.currencyCode ?? 'USD'}`. `session.currencyCode` is already in scope (same pattern already used for PersonResultsScreen:558 and TipScreen:570). This makes currencyCode REQUIRED — verify no other render site of ClaimableItemCard exists (grep) and fix it the same way if found.
    No data/API/store changes — the value is already on session. Implements CURR-01/CURR-02 display threading per UAT gap 1.
  </action>
  <verify>
    <automated>npx vitest run __tests__/ClaimableItemCard.test.tsx && npx tsc --noEmit && grep -n 'currencyCode' components/split/ClaimableItemCard.tsx | grep -v '^#' | grep -c currencyCode</automated>
  </verify>
  <done>ClaimableItemCardProps has a required `currencyCode: string`; both formatCents calls pass it; CollaborativeClaimingView passes `currencyCode={session.currencyCode ?? 'USD'}`; new EUR test passes (price shows €); tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Subtotal + in-card Total rows to Results breakdown (Gap 2 — minor)</name>
  <files>components/split/PersonResultsScreen.tsx, __tests__/PersonResultsScreen.test.tsx</files>
  <behavior>
    - RED: add a test to __tests__/PersonResultsScreen.test.tsx (reuse the existing render helper that passes all props, incl. currencyCode) asserting the current-user card renders a Subtotal row with `data-testid="results-subtotal"` whose value equals the items-only subtotal (share.itemSubtotal, e.g. existing fixture's item total), and an in-card Total row `data-testid="results-card-total"` equal to items + tip. Keep the existing `data-testid="results-tip"` assertion intact. This fails until the rows are added.
  </behavior>
  <action>
    In components/split/PersonResultsScreen.tsx current-user expanded block: insert an items-only Subtotal row immediately after the line-items <ul> closes (~:209), BEFORE the existing tip block (:211-225). The row mirrors the existing "Your tip" row markup (`className="flex justify-between text-[14px]"`): label "Subtotal", value `formatCents(share.itemSubtotal, currencyCode)`, `data-testid="results-subtotal"`. Then, inside the existing isCurrentUser tip block, AFTER the "Your tip" row, add an in-card Total row: a Separator (`my-2`) then a `flex justify-between` row with a semibold/emphasis class consistent with the existing styling, label "Total", value `formatCents(share.total, currencyCode)`, `data-testid="results-card-total"`. Reading order becomes: items -> Subtotal -> Your tip -> Total. Keep `data-testid="results-tip"` on the tip row unchanged.
    Optionally relabel the headline caption (:181) "Your share" -> "Your total" (it includes tip) — apply this since the headline = share.total; if any existing test asserts the exact text "Your share", update that test too.
    NO math changes — share.itemSubtotal / share.tip / share.total already come from computePersonShareFromClaims (billMath.ts:164-169). Implements UAT gap 2.
  </action>
  <verify>
    <automated>npx vitest run __tests__/PersonResultsScreen.test.tsx && npx tsc --noEmit && grep -c 'results-subtotal' components/split/PersonResultsScreen.tsx</automated>
  </verify>
  <done>Current-user breakdown renders Subtotal (items-only, data-testid="results-subtotal") and an in-card Total (items+tip, data-testid="results-card-total") row in items->Subtotal->Your tip->Total order; results-tip preserved; all PersonResultsScreen tests pass; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Clear persisted store sessionId on New Split (Gap 3 — major)</name>
  <files>components/split/PersonResultsScreen.tsx, __tests__/PersonResultsScreen.test.tsx</files>
  <behavior>
    - RED: add a test to __tests__/PersonResultsScreen.test.tsx that sets a sessionId on the store (`useBillStore.getState().setSessionId('sess-x')` or seed via reset+set), renders PersonResultsScreen, opens the New Split confirm dialog and clicks the confirm "New Split" button, then asserts `useBillStore.getState().sessionId` is null. Import `useBillStore` from '@/stores/useBillStore'. (router.push is already mocked via the existing next/navigation mock — confirm the mock exists in this test file; if not, mirror the useRouter mock pattern used in CollaborativeClaimingView.test.tsx.) This fails because handleNewSplit does not currently clear the store.
  </behavior>
  <action>
    In components/split/PersonResultsScreen.tsx `handleNewSplit` (:113-120): import `useBillStore` from '@/stores/useBillStore' (top of file). Inside handleNewSplit, BEFORE `router.push('/')` and after the localStorage.removeItem try/catch, call `useBillStore.getState().reset()`. Use the existing `reset()` action (stores/useBillStore.ts:142-146) — it returns the store to INITIAL_STATE, revokes any blob URL, and clears the persisted sessionId (partialize :169). Do NOT add a new store action; reset() already exists and is the correct fuller clear for starting a brand-new split. This stops app/page.tsx:22-26 from redirecting back to /split/{sessionId} on the first tap. Implements UAT gap 3.
  </action>
  <verify>
    <automated>npx vitest run __tests__/PersonResultsScreen.test.tsx && npx tsc --noEmit && grep -c 'useBillStore.getState().reset()' components/split/PersonResultsScreen.tsx</automated>
  </verify>
  <done>handleNewSplit calls `useBillStore.getState().reset()` before `router.push('/')`; new test confirms store sessionId is null after confirming New Split; all PersonResultsScreen tests pass; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session payload -> client render | currencyCode from SessionPayload is display-only here; server-side currency validation (Plan 02 /edit update_currency) remains the authoritative guard. |
| client -> localStorage / persisted store | New Split clears local identity + persisted store only; the shared Redis session is untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-12 | Tampering | currencyCode rendered in ClaimableItemCard | accept | currencyCode passed to formatCents -> Intl.NumberFormat; invalid codes fall back to `$` (billMath.ts:36). No HTML injection — value is a formatted number string, never dangerouslySetInnerHTML. |
| T-10-13 | Denial of Service | New Split reset() | accept | reset() only mutates local persisted store + revokes a blob URL; no network call, no Redis delete. Other participants' shared session unaffected (confirmed in 10-03-SUMMARY threat notes). |
</threat_model>

<verification>
- `npx vitest run __tests__/ClaimableItemCard.test.tsx __tests__/PersonResultsScreen.test.tsx` — all green
- `npx vitest run` full suite — no NEW failures (3 pre-existing wizard-test failures in AddPeopleStep/AddItemsStep are out of scope, documented in 10-04-SUMMARY)
- `npx tsc --noEmit` — Exit 0
- Grep gates:
  - `grep -c currencyCode components/split/ClaimableItemCard.tsx` >= 3 (prop + 2 formatCents calls)
  - `grep 'currencyCode={session.currencyCode' app/split/[sessionId]/CollaborativeClaimingView.tsx` — matches at ClaimableItemCard render site
  - `grep -c results-subtotal components/split/PersonResultsScreen.tsx` >= 1
  - `grep -c 'useBillStore.getState().reset()' components/split/PersonResultsScreen.tsx` == 1
</verification>

<success_criteria>
- Claiming / Bill View amounts show the session's currency symbol (e.g. ₺ for a TRY bill), not `$`, from the first claiming screen.
- Results current-user card reads items -> Subtotal -> Your tip -> Total with explicit Subtotal and Total rows.
- New Split lands on `/` on the first tap (persisted store sessionId cleared, so app/page.tsx no longer redirects back).
- No new test failures; tsc clean.
</success_criteria>

<output>
Create `.planning/phases/10-results-screen-tip-modal-currency-display/10-05-SUMMARY.md` when done.
</output>
