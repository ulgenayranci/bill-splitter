---
phase: 10-results-screen-tip-modal-currency-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/billMath.ts
  - __tests__/billMath.test.ts
autonomous: true
requirements: [CURR-02, CURR-03]
must_haves:
  truths:
    - "formatCents(1250) returns '$12.50' (backward-compatible, no currencyCode arg)"
    - "formatCents(1250, 'EUR') returns '€12.50'"
    - "formatCents(1250, 'JPY') returns '¥1,250' (zero-decimal — NOT divided by 100)"
    - "formatCents(1250, 'BOGUS') falls back gracefully to legacy '$X.XX' without throwing"
  artifacts:
    - path: "lib/billMath.ts"
      provides: "formatCents(cents, currencyCode?) with Intl.NumberFormat zero-decimal handling"
      contains: "currencyCode"
    - path: "__tests__/billMath.test.ts"
      provides: "4 new formatCents currency cases (EUR, JPY, GBP, fallback) + 3 unchanged legacy cases"
      contains: "JPY"
  key_links:
    - from: "lib/billMath.ts formatCents"
      to: "Intl.NumberFormat resolvedOptions().minimumFractionDigits"
      via: "divisor = Math.pow(10, minimumFractionDigits)"
      pattern: "minimumFractionDigits"
---

<objective>
Upgrade `formatCents` in `lib/billMath.ts` to accept an optional `currencyCode` parameter that uses `Intl.NumberFormat` to render the correct symbol and decimal places per ISO 4217 currency — including zero-decimal currencies like JPY and KRW (CURR-02). The no-argument path is preserved exactly so all ~10 existing call sites continue to receive the legacy `$X.XX` output unchanged. Invalid/empty currency inputs fall back to the legacy format gracefully (CURR-03).

Purpose: This is the foundation for all currency display in Phase 10. Plans 03 and 04 thread `session.currencyCode` into their `formatCents` calls; this plan makes that call signature correct first.
Output: Updated `formatCents` + 4 new passing tests in `__tests__/billMath.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-results-screen-tip-modal-currency-display/10-RESEARCH.md
@.planning/phases/10-results-screen-tip-modal-currency-display/10-PATTERNS.md

<interfaces>
<!-- Current formatCents (lib/billMath.ts lines 13-15) — the only function this plan changes: -->
Current: `export function formatCents(cents: number): string` → returns `` `$${(cents / 100).toFixed(2)}` ``

<!-- Verified Intl outputs (RESEARCH.md, Node.js runtime 2026-06-08): -->
- formatCents(1250) → "$12.50"   (no arg — legacy path)
- formatCents(1250, 'USD') → "$12.50"
- formatCents(1250, 'EUR') → "€12.50"
- formatCents(1250, 'JPY') → "¥1,250"   (minimumFractionDigits=0 → divisor=1 → no divide-by-100)
- formatCents(999, 'GBP') → "£9.99"
- formatCents(1250, 'BOGUS') → "BOGUS 12.50" (Intl does NOT throw on unknown codes; acceptable per D-06)
- formatCents(1250, '') → "$12.50"   (empty string throws in Intl → caught → legacy fallback)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing formatCents currency tests (RED)</name>
  <files>__tests__/billMath.test.ts</files>
  <read_first>
    - __tests__/billMath.test.ts (existing `describe('formatCents')` block ~lines 47-59 — see the 3 legacy assertions that MUST keep passing)
    - lib/billMath.ts (current single-arg formatCents at lines 13-15)
    - .planning/phases/10-results-screen-tip-modal-currency-display/10-RESEARCH.md (§"Critical Pattern: formatCents Upgrade" — verified output table)
  </read_first>
  <behavior>
    - Legacy unchanged: formatCents(1250) === '$12.50' (no-arg case still passes)
    - EUR: formatCents(1250, 'EUR') === '€12.50'
    - JPY zero-decimal: formatCents(1250, 'JPY') === '¥1,250' (asserts NOT '¥12' and NOT '¥12.50')
    - GBP: formatCents(999, 'GBP') === '£9.99'
    - Fallback on empty string: formatCents(1250, '') === '$12.50'
  </behavior>
  <action>
    In the existing `describe('formatCents', ...)` block of `__tests__/billMath.test.ts`, add 4 new `it(...)` cases for the second `currencyCode` argument: EUR ('€12.50'), JPY ('¥1,250' — explicitly assert it is not divided by 100), GBP ('£9.99'), and an empty-string fallback case asserting '$12.50'. Use the exact Unicode symbols (€, ¥, £) and the grouped thousands form for JPY ('¥1,250'). Do NOT modify the 3 existing legacy assertions — they prove backward compatibility (Pitfall 2 in RESEARCH.md). Run the test file and confirm the 4 new cases FAIL (because lib/billMath.ts has no currencyCode param yet) while the 3 legacy cases still pass.
  </action>
  <verify>
    <automated>npx vitest run __tests__/billMath.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `__tests__/billMath.test.ts` contains assertions referencing 'EUR', 'JPY', 'GBP', and an empty-string currency case
    - The JPY case asserts the result equals '¥1,250' (grouped, no decimal)
    - Running the test file shows the 4 new cases failing and the 3 pre-existing legacy formatCents cases passing
  </acceptance_criteria>
  <done>4 new failing currency-aware formatCents tests exist; legacy tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement currency-aware formatCents (GREEN)</name>
  <files>lib/billMath.ts</files>
  <read_first>
    - lib/billMath.ts (current formatCents at lines 13-15, plus surrounding exports computeSubtotalCents / computePersonShareFromClaims to match doc-comment style)
    - .planning/phases/10-results-screen-tip-modal-currency-display/10-RESEARCH.md (§"Code Examples" Pattern 1 — verified implementation)
    - .planning/phases/10-results-screen-tip-modal-currency-display/10-PATTERNS.md (§"lib/billMath.ts" — exact new signature + JSDoc)
  </read_first>
  <action>
    Change the `formatCents` signature to `export function formatCents(cents: number, currencyCode?: string): string`. Keep the existing early-return legacy path verbatim for the no-`currencyCode` case (`` return `$${(cents / 100).toFixed(2)}` ``) so the 3 legacy tests stay green (Pitfall 2). When `currencyCode` is provided, construct `new Intl.NumberFormat(undefined, { style: 'currency', currency: currencyCode })`, read `fmt.resolvedOptions().minimumFractionDigits` as `decimals`, compute `divisor = Math.pow(10, decimals)`, and return `fmt.format(cents / divisor)`. Wrap the Intl block in try/catch; on catch, return the legacy `` `$${(cents / 100).toFixed(2)}` `` fallback (catches the empty-string / null throw cases per Pitfall 6; unknown codes like 'BOGUS' do NOT throw and render 'BOGUS 12.50', which is acceptable per D-06). Update the JSDoc to document the backward-compatible behavior and zero-decimal handling. Do not change any other export in the file.
  </action>
  <verify>
    <automated>npx vitest run __tests__/billMath.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `lib/billMath.ts` formatCents signature is `formatCents(cents: number, currencyCode?: string): string`
    - The no-arg branch still returns `$${(cents / 100).toFixed(2)}` form (legacy path preserved)
    - Implementation reads `resolvedOptions().minimumFractionDigits` and uses `Math.pow(10, decimals)` as divisor
    - `npx vitest run __tests__/billMath.test.ts` exits 0 (all 7 cases green: 3 legacy + 4 new)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>formatCents handles all D-06 currencies with correct decimals; legacy and new tests all pass; no type errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session payload → formatCents | `currencyCode` from Redis (set by OCR or by a participant via update_currency) is rendered into the UI string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | formatCents currencyCode arg | mitigate | try/catch around Intl.NumberFormat; null/empty/throwing inputs fall back to legacy '$X.XX'. Unknown ISO codes render as "CODE 12.50" (no script injection — output is a plain string rendered as React text, auto-escaped) |
| T-10-02 | Denial of Service | formatCents called per line item | accept | Pure synchronous string format, O(1) per call; no unbounded loops; input cents is an integer already validated upstream |
</threat_model>

<verification>
- `npx vitest run __tests__/billMath.test.ts` — all formatCents cases green (3 legacy + 4 new)
- `npx tsc --noEmit` — no type errors
- Grep gate: `grep -v '^[[:space:]]*//' lib/billMath.ts | grep -c minimumFractionDigits` returns ≥1
</verification>

<success_criteria>
- formatCents renders correct symbol + decimals for USD, EUR, GBP, JPY (zero-decimal), and falls back to legacy on invalid input
- All existing call sites (no second arg) produce identical output to before this plan
- Full test file green; no TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/10-results-screen-tip-modal-currency-display/10-01-SUMMARY.md` when done
</output>
