---
phase: 07-app-shell-setup-screen
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - components/wizard/AppHeader.tsx
  - components/wizard/WizardShell.tsx
  - app/page.tsx
  - __tests__/WizardShell.test.tsx
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
  - SHELL-03
  - SHELL-04
must_haves:
  truths:
    - "AppHeader renders the easy-billsy wordmark on an element with aria-label=\"easy-billsy\" (SHELL-01)"
    - "WizardShell mounts <AppHeader /> so the header appears on every wizard screen (SHELL-01)"
    - "The hamburger menu lists exactly three rows: New Split (button), History (disabled div), About Us (disabled div) (SHELL-02, D-04, D-06)"
    - "New Split calls reset() then setStep(1); a confirm-reset Dialog appears first when a split is already in progress (SHELL-03, D-05)"
    - "History and About Us are non-tappable greyed divs with aria-disabled=\"true\" and text-zinc-300 — no onClick, no coming-soon state (D-06)"
    - "WizardShell renders a 3-segment progress strip (PROGRESS_SEGMENTS = 3), replacing the v1 4-segment strip (SHELL-04, D-07)"
    - "Only the first segment is filled on the Setup step (filledSegments(1) === 1) (SHELL-04)"
  artifacts:
    - path: "components/wizard/AppHeader.tsx"
      provides: "Style A branded header (white bg, dark wordmark, amber hamburger) + dropdown menu + confirm-reset dialog"
      contains: "aria-label=\"easy-billsy\""
    - path: "components/wizard/WizardShell.tsx"
      provides: "3-segment progress strip + AppHeader mount + step↔hash sync"
      contains: "const PROGRESS_SEGMENTS = 3"
    - path: "app/page.tsx"
      provides: "WizardShell wrapper around step-routed content"
      contains: "<WizardShell>"
    - path: "__tests__/WizardShell.test.tsx"
      provides: "Tests asserting 3 segments, header presence, first-segment-filled on Setup"
      contains: "renders 3 progress strip segments"
  key_links:
    - from: "components/wizard/WizardShell.tsx"
      to: "components/wizard/AppHeader.tsx"
      via: "import { AppHeader } and <AppHeader /> render"
      pattern: "import \\{ AppHeader \\} from './AppHeader'"
    - from: "components/wizard/AppHeader.tsx"
      to: "stores/useBillStore.ts"
      via: "reset + setStep drive New Split; people/items/billImageUrl drive hasProgress"
      pattern: "const reset = useBillStore"
---

<objective>
Document the shipped easy-billsy app shell: the branded Style A header present on every screen, the hamburger menu (New Split active; History + About Us disabled), the New Split reset flow with confirm-on-progress, and the 3-segment progress strip that replaced the v1 4-segment wizard strip.

Purpose: complete the GSD planning trail so a downstream verifier can check SHELL-01..04 against the shipped code via the must_haves above.
Output: this retroactive PLAN.md documenting AppHeader.tsx, WizardShell.tsx, app/page.tsx, and the shell tests.

**Retroactive:** documents work shipped in commits cb10468 / 430b02f; no execution required.

This plan covers: SHELL-01 (wordmark on every screen), SHELL-02 (hamburger New Split / History / About Us), SHELL-03 (New Split resets), SHELL-04 (3-step progress indicator). Implements D-03 (Style A header), D-04 (menu styling), D-05 (New Split with confirm-on-progress), D-06 (History + About Us disabled), D-07 (3-segment strip).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-app-shell-setup-screen/07-CONTEXT.md
@components/wizard/AppHeader.tsx
@components/wizard/WizardShell.tsx
@app/page.tsx
@__tests__/WizardShell.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: easy-billsy Style A header + hamburger menu (AppHeader.tsx)</name>
  <files>components/wizard/AppHeader.tsx</files>
  <read_first>
    - components/wizard/AppHeader.tsx (full shipped file)
    - .planning/phases/07-app-shell-setup-screen/07-CONTEXT.md (D-03, D-04, D-05, D-06)
  </read_first>
  <action>WHAT WAS BUILT (retroactive — already shipped in cb10468): AppHeader renders the Style A header — white background, the dark `easy−billsy` wordmark (aria-label="easy-billsy"), and an amber three-bar hamburger toggle (aria-label="Menu"). Opening the hamburger shows a ~168px dropdown (role="menu") with three rows: an active New Split button (onClick={handleNewSplit}), and two greyed, non-tappable `<div role="menuitem" aria-disabled="true">` rows for History and About Us (D-06). No execution required; verify against shipped code via acceptance_criteria.</action>
  <acceptance_criteria>
    - File `components/wizard/AppHeader.tsx` exists and exports `function AppHeader`.
    - `grep -c 'aria-label="easy-billsy"' components/wizard/AppHeader.tsx` returns `1` (SHELL-01 wordmark).
    - The wordmark renders three spans: `>easy<`, the `−` separator span, and `>billsy<` (`grep -c "font-bold" components/wizard/AppHeader.tsx` returns at least `1`).
    - `grep -c 'aria-label="Menu"' components/wizard/AppHeader.tsx` returns `1` (hamburger toggle).
    - The hamburger draws three amber bars: `grep -c "bg-amber-600" components/wizard/AppHeader.tsx` returns at least `3` (per D-03 amber hamburger lines).
    - Menu contains a New Split button: `grep -c "New Split" components/wizard/AppHeader.tsx` returns at least `1`, and it has `onClick={handleNewSplit}`.
    - History and About Us are rendered as disabled rows: `grep -c 'aria-disabled="true"' components/wizard/AppHeader.tsx` returns `2` and `grep -c "text-zinc-300" components/wizard/AppHeader.tsx` returns at least `2` (D-06 greyed/non-tappable).
    - Neither History nor About Us has an `onClick` handler (they are `<div role="menuitem">`, not `<button>`).
    - Menu container width is ~168px: `grep -c "w-\[168px\]" components/wizard/AppHeader.tsx` returns `1` (D-04).
  </acceptance_criteria>
  <done>AppHeader exposes the easy-billsy wordmark (aria-label="easy-billsy"), an amber 3-bar hamburger, and a 168px dropdown with an active New Split and two disabled rows (History, About Us).</done>
</task>

<task type="auto">
  <name>Task 2: New Split reset flow with confirm-on-progress (AppHeader.tsx)</name>
  <files>components/wizard/AppHeader.tsx</files>
  <read_first>
    - components/wizard/AppHeader.tsx (handleNewSplit, startNewSplit, hasProgress, confirm dialog)
    - .planning/phases/07-app-shell-setup-screen/07-CONTEXT.md (D-05)
    - stores/useBillStore.ts (reset, setStep, people/items/billImageUrl)
  </read_first>
  <action>WHAT WAS BUILT (retroactive — already shipped in cb10468): New Split runs `startNewSplit()` which calls `reset()` then `setStep(1)`. `handleNewSplit` first checks `hasProgress` (`people.length > 0 || items.length > 0 || billImageUrl !== null`); if a split is in progress it opens a confirm Dialog titled "Start a new split?" (D-05), otherwise it resets immediately. The dialog's confirm button calls `startNewSplit()` after closing. No execution required; verify against shipped code.</action>
  <acceptance_criteria>
    - `startNewSplit` calls `reset()` then `setStep(1)`: `grep -c "reset()" components/wizard/AppHeader.tsx` returns at least `1` and `grep -c "setStep(1)" components/wizard/AppHeader.tsx` returns at least `1` (SHELL-03).
    - `hasProgress` is derived from `people.length > 0 || items.length > 0 || billImageUrl !== null` (D-05 "a split is in progress once anything has been added").
    - `handleNewSplit` shows the confirm dialog only when `hasProgress` is true, else calls `startNewSplit()` directly: `grep -c "setConfirmReset(true)" components/wizard/AppHeader.tsx` returns at least `1`.
    - A confirm Dialog with title "Start a new split?" is present: `grep -c "Start a new split" components/wizard/AppHeader.tsx` returns at least `1`.
    - Confirm dialog's New Split button calls `startNewSplit()` after closing: `grep -c "startNewSplit()" components/wizard/AppHeader.tsx` returns at least `2` (direct path + confirmed path).
  </acceptance_criteria>
  <done>New Split resets the store and returns to step 1; a confirm-reset Dialog gates the action whenever people, items, or a bill photo already exist.</done>
</task>

<task type="auto">
  <name>Task 3: 3-segment progress strip + header mount (WizardShell.tsx, app/page.tsx, WizardShell.test.tsx)</name>
  <files>components/wizard/WizardShell.tsx, app/page.tsx, __tests__/WizardShell.test.tsx</files>
  <read_first>
    - components/wizard/WizardShell.tsx (PROGRESS_SEGMENTS, filledSegments, AppHeader mount, hash sync)
    - app/page.tsx (WizardShell wrapper + step routing)
    - __tests__/WizardShell.test.tsx (shipped shell tests)
    - .planning/phases/07-app-shell-setup-screen/07-CONTEXT.md (D-07)
  </read_first>
  <action>WHAT WAS BUILT (retroactive — already shipped in cb10468): WizardShell mounts `<AppHeader />` (so the header shows on every screen) above a 3-segment progress strip (`const PROGRESS_SEGMENTS = 3`, D-07). `filledSegments(step)` maps step ≥ 4 → 3, step ≥ 3 → 2, else 1, so Setup (step 1) fills exactly the first segment. `app/page.tsx` wraps the step-routed content in `<WizardShell>`. `__tests__/WizardShell.test.tsx` asserts 3 segments, header presence, and one filled segment on Setup. No execution required; verify against shipped code.</action>
  <acceptance_criteria>
    - `grep -c "const PROGRESS_SEGMENTS = 3" components/wizard/WizardShell.tsx` returns `1` (SHELL-04, D-07 — 3 segments replacing v1's 4).
    - WizardShell mounts the header: `grep -c "<AppHeader />" components/wizard/WizardShell.tsx` returns `1` and `grep -c "import { AppHeader } from './AppHeader'" components/wizard/WizardShell.tsx` returns `1` (SHELL-01 on every screen).
    - `filledSegments` maps step→fill as: step ≥ 4 → 3, step ≥ 3 → 2, else 1 (Setup active fills exactly the first segment).
    - The strip renders `PROGRESS_SEGMENTS` divs via `Array.from({ length: PROGRESS_SEGMENTS }` and toggles `bg-amber-600` vs `bg-zinc-200` by fill.
    - `app/page.tsx` wraps content in `<WizardShell>`: `grep -c "<WizardShell>" app/page.tsx` returns `1`.
    - The shipped test `__tests__/WizardShell.test.tsx` asserts exactly 3 segments, header presence (aria-label "easy-billsy" + "Menu"), and a single filled segment on step 1. Running it passes: `npx vitest run __tests__/WizardShell.test.tsx` exits 0.
  </acceptance_criteria>
  <done>WizardShell renders a 3-segment strip with AppHeader mounted above it; page.tsx wraps the step-routed content; the shell test suite passes against the shipped component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → store | AppHeader reads/writes only client-side Zustand state (reset, setStep). No network or untrusted external input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01-01 | Tampering | AppHeader New Split reset | accept | Reset clears only client-local session state; no server data destroyed. Confirm-reset Dialog (D-05) guards accidental loss of in-progress work. |
| T-7-01-02 | Denial of service | WizardShell hashchange listener | accept | Listener narrows to `#step-([1-4])` and clamps to 1–4; malformed hashes are ignored, no unbounded state writes. |
</threat_model>

<verification>
After documenting:
- `npx vitest run __tests__/WizardShell.test.tsx` passes (3 segments, header present, first segment filled on Setup).
- `grep -c 'aria-label="easy-billsy"' components/wizard/AppHeader.tsx` returns 1 (SHELL-01).
- `grep -c 'aria-disabled="true"' components/wizard/AppHeader.tsx` returns 2 (History + About Us disabled, D-06).
- `grep -c "const PROGRESS_SEGMENTS = 3" components/wizard/WizardShell.tsx` returns 1 (SHELL-04).
</verification>

<success_criteria>
- The easy-billsy wordmark (aria-label="easy-billsy") renders on every screen via WizardShell → AppHeader (SHELL-01).
- The hamburger menu offers New Split (active), History (disabled), About Us (disabled) per D-04/D-06 (SHELL-02).
- New Split resets the bill and returns to Setup, confirming first when a split is in progress (SHELL-03, D-05).
- The progress strip has 3 segments (Setup / Bill View / Results), Setup filling segment one (SHELL-04, D-07).
</success_criteria>

<output>
Retroactive plan — no SUMMARY required. The shipped commits cb10468 / 430b02f are the implementation of record.
</output>
