---
phase: 09-bill-view-redesign-identity-modal
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - components/split/BillViewHeader.tsx
  - components/split/UnclaimedBanner.tsx
  - __tests__/BillViewHeader.test.tsx
  - __tests__/UnclaimedBanner.test.tsx
autonomous: true
requirements: [IDENT-03, CLAIM-05, CLAIM-06]
must_haves:
  truths:
    - "The Bill View header shows a bill title with date, the people strip, and receipt + share icons"
    - "Tapping anywhere on the people strip fires the change-identity callback"
    - "The header's share icon lets others get the join link so they can claim on their own phones"
    - "A banner shows the live count of unclaimed items and is hidden when everything is claimed"
    - "Tapping the banner invokes the scroll-to-first-unclaimed callback"
  artifacts:
    - path: "components/split/BillViewHeader.tsx"
      provides: "title+date, people strip (own pill + compact circles + +N), receipt + share icons"
      contains: "onStripTap"
    - path: "components/split/UnclaimedBanner.tsx"
      provides: "live unclaimed counter banner, hidden when 0 unclaimed"
      contains: "still unclaimed"
  key_links:
    - from: "components/split/BillViewHeader.tsx people strip"
      to: "onStripTap prop"
      via: "tappable strip container (D-03 change identity)"
      pattern: "onStripTap"
    - from: "components/split/BillViewHeader.tsx share icon"
      to: "ShareLinkButton / share action"
      via: "header share affordance (D-11, CLAIM-06)"
      pattern: "Share2"
    - from: "components/split/UnclaimedBanner.tsx"
      to: "onTap prop"
      via: "scroll-to-first-unclaimed (D-10)"
      pattern: "onTap"
---

<objective>
Build the two new presentational Bill View chrome components: BillViewHeader (title + date,
people strip with own-identity pill / compact circles / +N overflow, receipt + share icons)
and UnclaimedBanner (live "N of M still unclaimed" counter). Both are pure display components
driven by SWR session data; the orchestrator (Plan 06) mounts them and provides callbacks.

Purpose: the header carries the change-identity tap target (D-03/IDENT-03) and the header
share icon (D-11/CLAIM-06); the banner surfaces unclaimed items before results (CLAIM-05/D-10).
Output: BillViewHeader.tsx + UnclaimedBanner.tsx with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-bill-view-redesign-identity-modal/09-CONTEXT.md
@.planning/phases/09-bill-view-redesign-identity-modal/09-PATTERNS.md
@.planning/phases/09-bill-view-redesign-identity-modal/09-UI-SPEC.md

<interfaces>
From lib/sessionSchema.ts: `SessionPayload` (people[], items[], claims.items, claims.personSlots, createdAt: number)
From stores/useBillStore.ts: `PersonId`, `Person { id, name, colorIndex }`, `AVATAR_COLORS` (6 entries), `Item { id, name, priceCents, quantity? }`
From components/wizard/ShareLinkButton.tsx: existing share/copy-link logic to reuse for the header share icon
Icons: lucide-react `Share2`, `Receipt`
Avatar color lookup (consistent across strip + card chips): `AVATAR_COLORS[(person?.colorIndex ?? 0) % AVATAR_COLORS.length] ?? AVATAR_COLORS[0]`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create BillViewHeader (title/date, people strip, receipt + share icons)</name>
  <files>components/split/BillViewHeader.tsx, __tests__/BillViewHeader.test.tsx</files>
  <read_first>
    - components/wizard/AppHeader.tsx (sticky header bar pattern, Dialog-based actions, amber accent — header sits BELOW AppHeader)
    - components/split/ClaimableItemCard.tsx (avatar chip + overflow pattern, AVATAR_COLORS lookup ~lines 154-158, MAX cap pattern)
    - components/wizard/ShareLinkButton.tsx (FULL — reuse for the share icon action; do not reinvent clipboard/navigator.share)
    - stores/useBillStore.ts (AVATAR_COLORS, Person.colorIndex)
    - .planning/phases/09-bill-view-redesign-identity-modal/09-UI-SPEC.md (People Strip Visual Contract + Bill title block + Header right icons + Copywriting: aria-labels "View receipt", "Share bill link", "People — tap to change identity")
    - .planning/phases/09-bill-view-redesign-identity-modal/09-RESEARCH.md (Pitfall 7 — NO merchantName in schema; use "Bill — {Mon DD}" fallback; formatBillDate from session.createdAt)
    - .planning/phases/09-bill-view-redesign-identity-modal/09-design-bill-view-header.png (visual anchor)
  </read_first>
  <behavior>
    - Test: header renders a bill title containing "Bill —" and a date derived from session.createdAt
    - Test: own identity (myPersonId) renders as an expanded pill showing the person's name; others render as compact initial circles
    - Test: when there are more than 3 other people, an overflow "+N" badge shows the correct N
    - Test: tapping the people strip container calls onStripTap
    - Test: a share affordance with aria-label "Share bill link" is present; a receipt affordance with aria-label "View receipt" is present
    - Test: avatar color class for a person matches AVATAR_COLORS[colorIndex % 6]
  </behavior>
  <action>
    Create components/split/BillViewHeader.tsx (`'use client'`) with props
    `{ session: SessionPayload; myPersonId: PersonId | null; onStripTap: () => void; sessionId: string }`.
    Render a sticky header below AppHeader: Row 1 = bill title `Bill — ${formatBillDate(session.createdAt)}`
    (`text-[20px] font-semibold text-zinc-900`) with a date line below, plus right-aligned receipt
    (`lucide Receipt` size 22, aria-label "View receipt") and share (`lucide Share2` size 22,
    aria-label "Share bill link") icons in a `flex items-center gap-3 ml-auto` container; wire the share
    icon to ShareLinkButton's copy/share logic using sessionId. Row 2 = people strip: a tappable container
    (`cursor-pointer`, aria-label "People — tap to change identity", onClick=onStripTap) showing the
    own-identity person as an expanded amber pill (`h-8 rounded-full bg-amber-50 border border-amber-400`,
    avatar circle + name `text-[14px] font-semibold`), other people as compact `h-8 w-8 rounded-full`
    circles in their AVATAR_COLOR with white initial `text-[11px] font-semibold`, capped at MAX_STRIP_AVATARS=3
    others then a `+N` badge (`h-8 w-8 rounded-full bg-zinc-100`, `text-[14px] font-semibold text-zinc-500`).
    Add `formatBillDate(createdAt: number)` using `new Date(createdAt).toLocaleDateString('en-US', { month:'short', day:'numeric' })`.
    Do NOT add a merchantName field anywhere. Write failing tests first, then implement.
  </action>
  <verify>
    <automated>npx vitest run __tests__/BillViewHeader.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run __tests__/BillViewHeader.test.tsx` passes title/date, own-pill-vs-circles, +N overflow, onStripTap, and aria-label tests
    - `grep -c "merchantName" components/split/BillViewHeader.tsx` returns 0 (Pitfall 7)
    - `grep -n "Share bill link" components/split/BillViewHeader.tsx` and `grep -n "View receipt" components/split/BillViewHeader.tsx` each return a match
    - `grep -n "MAX_STRIP_AVATARS" components/split/BillViewHeader.tsx` shows cap = 3
  </acceptance_criteria>
  <done>BillViewHeader renders the title/date, tappable people strip (own pill + circles + +N), and receipt/share icons with correct aria-labels and avatar colors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create UnclaimedBanner live counter</name>
  <files>components/split/UnclaimedBanner.tsx, __tests__/UnclaimedBanner.test.tsx</files>
  <read_first>
    - app/split/[sessionId]/CollaborativeClaimingView.tsx (the existing allItemsFullyClaimed traversal ~lines 46-52 — mirror it for the unclaimed count)
    - components/split/ClaimableItemCard.tsx (conditional-render-when-condition-true pattern; tinted-banner styling reference)
    - .planning/phases/09-bill-view-redesign-identity-modal/09-UI-SPEC.md (Unclaimed Banner Visual Contract + Copywriting: "1 item still unclaimed — tap to find it" / "N of M items still unclaimed — tap to find them")
  </read_first>
  <behavior>
    - Test: when all items are claimed (unclaimed === 0) the component renders nothing (null)
    - Test: when exactly 1 item is unclaimed, copy is "1 item still unclaimed — tap to find it"
    - Test: when N>1 items are unclaimed of M total, copy is "N of M items still unclaimed — tap to find them"
    - Test: tapping the banner calls onTap
    - Test: an item with total claimed qty < item.quantity counts as unclaimed; total >= quantity does not
  </behavior>
  <action>
    Create components/split/UnclaimedBanner.tsx (`'use client'`) with props `{ session: SessionPayload; onTap: () => void }`.
    Add an internal `getUnclaimedCounts(session)` that iterates session.items, sums claimed qty per item
    from `session.claims?.items?.[item.id]` (reduce e.qty), and counts an item as unclaimed when
    `totalClaimed < (item.quantity ?? 1)`; returns `{ unclaimed, total: session.items.length }`.
    If `unclaimed === 0` return null. Otherwise render a tappable banner (onClick=onTap) styled
    `flex items-center justify-between gap-2 px-4 py-2 bg-amber-50 border-b border-amber-200 text-[14px] text-amber-800 cursor-pointer`
    with copy: singular "1 item still unclaimed — tap to find it" when unclaimed===1, otherwise
    "{unclaimed} of {total} items still unclaimed — tap to find them". Write failing tests first, then implement.
  </action>
  <verify>
    <automated>npx vitest run __tests__/UnclaimedBanner.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run __tests__/UnclaimedBanner.test.tsx` passes the null-when-zero, singular, plural, onTap, and count-threshold tests
    - `grep -n "still unclaimed" components/split/UnclaimedBanner.tsx` returns both copy variants
    - Banner returns null (renders nothing) when unclaimed === 0 (asserted by test)
  </acceptance_criteria>
  <done>UnclaimedBanner computes live unclaimed counts, hides at zero, and fires onTap for scroll-to-first.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SWR session data → header/banner | Read-only display of polled session state; no writes from these components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Information Disclosure | share icon exposes the join link | accept | Sharing the link is the intended capability (CLAIM-06); session is anonymous, 24h TTL; no PII beyond display names |
| T-09-11 | Tampering | rendered names/counts from polled data | accept | Components are read-only; display names are validated at write time (Plan 02); no execution of session content |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH audit: none); lucide-react + ShareLinkButton already present |
</threat_model>

<verification>
- `npx vitest run __tests__/BillViewHeader.test.tsx __tests__/UnclaimedBanner.test.tsx` green
- `npx tsc --noEmit` no errors in the two new files
</verification>

<success_criteria>
- BillViewHeader + UnclaimedBanner exist, tested, with correct copy, aria-labels, avatar colors, and tap callbacks
- No merchantName introduced; share + receipt affordances present
</success_criteria>

<output>
Create `.planning/phases/09-bill-view-redesign-identity-modal/09-04-SUMMARY.md` when done
</output>
