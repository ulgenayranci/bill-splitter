---
phase: 09-bill-view-redesign-identity-modal
plan: 05
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - components/split/ClaimableItemCard.tsx
  - __tests__/ClaimableItemCard.test.tsx
autonomous: true
requirements: [CLAIM-02, CLAIM-04]
must_haves:
  truths:
    - "Every claimed item shows small colored avatar chips for its claimants (no name text), capped at 3 then +N"
    - "The current user's own-claimed card has an amber tint AND amber border"
    - "Tapping a single-quantity item joins or leaves its sharers via the share action (not the qty action)"
    - "A shared single-quantity item the user has joined shows 'your share: $X.XX' computed by equal split"
  artifacts:
    - path: "components/split/ClaimableItemCard.tsx"
      provides: "attribution chips (max 3 +N), own-claim amber border, onShareChange tap-to-join, your-share line"
      contains: "onShareChange"
  key_links:
    - from: "components/split/ClaimableItemCard.tsx"
      to: "lib/billMath.ts computeEqualShareCents"
      via: "your-share line for shared single-qty items (D-15)"
      pattern: "computeEqualShareCents"
    - from: "components/split/ClaimableItemCard.tsx single-qty tap"
      to: "onShareChange prop (handled by orchestrator → share action)"
      via: "tap-to-join/leave (D-13)"
      pattern: "onShareChange"
---

<objective>
Augment ClaimableItemCard for flat collaborative attribution: cap visible chips at 3 + "+N"
(D-07), add the own-claim amber border (D-06), route single-quantity taps through a new
`onShareChange` callback for tap-to-join/leave (D-13), and show "your share: $X.XX" using the
equal-split helper from Plan 01 (D-15). Multi-quantity stepper behavior is untouched (D-14).

Purpose: this is the live-attribution (CLAIM-04) and single-item-sharing (CLAIM-02) surface on
each card. The orchestrator (Plan 06) supplies onShareChange; this plan owns card rendering only.
Output: augmented ClaimableItemCard.tsx with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-bill-view-redesign-identity-modal/09-CONTEXT.md
@.planning/phases/09-bill-view-redesign-identity-modal/09-PATTERNS.md
@.planning/phases/09-bill-view-redesign-identity-modal/09-UI-SPEC.md
@.planning/phases/09-bill-view-redesign-identity-modal/09-01-SUMMARY.md

<interfaces>
From lib/billMath.ts (delivered by Plan 01):
- `computeEqualShareCents(priceCents: number, numSharers: number, myIndex: number): number`
  Determinism: caller sorts claimant personIds lexicographically ascending; lowest gets the extra cent.
From stores/useBillStore.ts: `PersonId`, `AVATAR_COLORS`, `Item { id, name, priceCents, quantity? }`
ClaimableItemCard currently: `MAX_VISIBLE_AVATARS = 5`; own-claim tint `bg-amber-50`; single-qty tap calls onQtyChange.
isMultiQty = `(item.quantity ?? 1) > 1`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chips to 3 +N, own-claim amber border, onShareChange tap-to-join, your-share line</name>
  <files>components/split/ClaimableItemCard.tsx, __tests__/ClaimableItemCard.test.tsx</files>
  <read_first>
    - components/split/ClaimableItemCard.tsx (FULL FILE — current props interface ~lines 11-18, MAX_VISIBLE_AVATARS=5 ~line 20, handleToggle ~lines 49-52, cardClasses own-claim `bg-amber-50` ~lines 65-68, chip row ~lines 146-178, AVATAR_COLORS lookup ~lines 154-158)
    - __tests__/ClaimableItemCard.test.tsx (existing chip-row tests — verify D-05/06/07 compliance; mirror its render/query structure for new cases)
    - lib/billMath.ts (computeEqualShareCents signature + determinism rule from Plan 01)
    - .planning/phases/09-bill-view-redesign-identity-modal/09-UI-SPEC.md (Item Card Attribution Visual Contract: chip h-6 w-6, max 3 +N, my chip ring-2 ring-amber-600, "your share: $X.XX" style text-[14px] text-zinc-500 mt-1, own-card bg-amber-50 + border-amber-400)
    - .planning/phases/09-bill-view-redesign-identity-modal/09-RESEARCH.md (Pattern 2 tap-to-join vs qty; Critical Decision A data representation — each sharer holds {qty:1})
  </read_first>
  <behavior>
    - Test (D-07): with >3 claimants, exactly 3 chips render plus a "+N" overflow badge with correct N
    - Test (D-06): when mine is true, the card root has both `bg-amber-50` and an amber border class (border-amber-400)
    - Test (D-13 single-qty, unclaimed): tapping a qty:1 card with myQty===0 calls onShareChange(true) and does NOT call onQtyChange
    - Test (D-13 single-qty, already mine): tapping when myQty>0 calls onShareChange(false)
    - Test (D-14 multi-qty unaffected): a multi-qty card (quantity>1) still uses the stepper / onQtyChange path; onShareChange not invoked by the stepper
    - Test (D-15): for a shared single-qty item where mine && claimants>1, a "your share:" line renders using computeEqualShareCents (e.g. $10.00 / 3 with my sorted index)
    - Test: when onShareChange is undefined, single-qty tap falls back to onQtyChange (backward compatible)
  </behavior>
  <action>
    Change `MAX_VISIBLE_AVATARS` from 5 to 3 (D-07).
    Augment the own-claim card class so `mine` adds BOTH `bg-amber-50` and `border-amber-400` (D-06).
    Add optional prop `onShareChange?: (joining: boolean) => void` to the props interface (undefined for multi-qty).
    Update `handleToggle`: if `onShareChange` is provided, call `onShareChange(myQty === 0)` (join when unclaimed,
    leave when already mine); otherwise keep the existing `onQtyChange(myQty === 0 ? 1 : 0)` fallback. Keep the
    multi-qty stepper (+/−) path entirely unchanged (D-14).
    Import `computeEqualShareCents` from '@/lib/billMath' and, after the chip row, when
    `!isMultiQty && mine && claimantCount > 1`, render `your share: {formatCents(computeEqualShareCents(item.priceCents, claimantCount, myIndex))}`
    where myIndex is the position of myPersonId within the claimant personIds sorted ascending (lexicographic),
    styled `text-[14px] text-zinc-500 mt-1` with `data-testid="your-share"`.
    Write the failing tests first, then implement.
  </action>
  <verify>
    <automated>npx vitest run __tests__/ClaimableItemCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run __tests__/ClaimableItemCard.test.tsx` passes the chips-cap-3, amber-border, tap-to-join join/leave, multi-qty-unchanged, your-share, and fallback tests
    - `grep -n "MAX_VISIBLE_AVATARS = 3" components/split/ClaimableItemCard.tsx` returns a match (no `= 5` remains: `grep -c "MAX_VISIBLE_AVATARS = 5"` returns 0)
    - `grep -n "onShareChange" components/split/ClaimableItemCard.tsx` returns the prop and the handleToggle call
    - `grep -n "computeEqualShareCents" components/split/ClaimableItemCard.tsx` returns the import and the your-share usage
    - `grep -n "border-amber-400" components/split/ClaimableItemCard.tsx` returns the own-claim border
  </acceptance_criteria>
  <done>Card shows ≤3 chips + N, own-claim amber border, single-qty tap routes through onShareChange, and your-share line uses equal-split math; multi-qty stepper unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SWR session data → card | Read-only render of claimants; the tap triggers a callback the orchestrator turns into a validated share write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12 | Tampering | tap-to-join issuing share writes | mitigate | Card only emits onShareChange(boolean); the actual write is the atomic, validated share action (Plan 01) on the server |
| T-09-13 | Information Disclosure | your-share computed client-side | accept | Share math is derived from public session data; no secret; both devices converge within the 3s poll |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH audit: none) |
</threat_model>

<verification>
- `npx vitest run __tests__/ClaimableItemCard.test.tsx __tests__/billMath.test.ts` green
- `npx tsc --noEmit` no errors in ClaimableItemCard.tsx
</verification>

<success_criteria>
- Chips capped at 3 + N; own-claim amber border; onShareChange tap-to-join; your-share via computeEqualShareCents
- Multi-qty stepper behavior unchanged (D-14)
</success_criteria>

<output>
Create `.planning/phases/09-bill-view-redesign-identity-modal/09-05-SUMMARY.md` when done
</output>
