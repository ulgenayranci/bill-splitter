---
phase: 09-bill-view-redesign-identity-modal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/session/[sessionId]/edit/route.ts
  - __tests__/editRoute.test.ts
autonomous: true
requirements: [IDENT-03]
must_haves:
  truths:
    - "Calling /edit with op:add_person creates a new person and returns their generated personId"
    - "The new person's identity slot is locked atomically so no other device can pick it (D-02)"
    - "Adding a person beyond the session cap (20) is rejected, not silently accepted"
  artifacts:
    - path: "app/api/session/[sessionId]/edit/route.ts"
      provides: "add_person op with ADD_PERSON_SCRIPT Lua (atomic person create + slot lock)"
      contains: "ADD_PERSON_SCRIPT"
  key_links:
    - from: "app/api/session/[sessionId]/edit/route.ts add_person"
      to: "redis.eval(ADD_PERSON_SCRIPT)"
      via: "atomic person append + personSlots lock"
      pattern: "op === 'add_person'"
    - from: "edit route add_person response"
      to: "CollaborativeClaimingView handleAddPerson (Plan 06)"
      via: "{ ok, personId } JSON the client sets as identity"
      pattern: "personId"
---

<objective>
Add the `add_person` operation to the /edit route so the "I'm not listed" path (IDENT-03)
can create a new participant for everyone (D-02) and immediately claim them as the caller's
identity. The op must be atomic (person create + slot lock in one Lua write) to avoid the
append race in Pitfall 2.

Purpose: "I'm not listed" needs a server-generated personId returned to the client, plus an
atomic slot lock so a concurrent second device cannot grab the new slot.
Output: add_person op on /edit with ADD_PERSON_SCRIPT Lua + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-bill-view-redesign-identity-modal/09-CONTEXT.md
@.planning/phases/09-bill-view-redesign-identity-modal/09-RESEARCH.md
@.planning/phases/09-bill-view-redesign-identity-modal/09-PATTERNS.md

<interfaces>
From stores/useBillStore.ts:
- `interface Person { id: PersonId; name: string; colorIndex: number }` (colorIndex cycles % 6)
- `AVATAR_COLORS` is a 6-entry array → colorIndex = (#people) % 6
- `randomId()` / `nanoid` available for server-side ID generation

From lib/sessionSchema.ts:
- `claims.personSlots: Record<PersonId, boolean>` — slot lock map
- `people: Person[]`

The /edit route currently has `VALID_OPS = ['add', 'remove', 'edit_price', 'edit_name', 'edit_quantity']`
and uses GET→mutate→SET (last-write-wins) for item ops. add_person needs Lua for atomicity.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add add_person op with ADD_PERSON_SCRIPT atomic Lua</name>
  <files>app/api/session/[sessionId]/edit/route.ts, __tests__/editRoute.test.ts</files>
  <read_first>
    - app/api/session/[sessionId]/edit/route.ts (FULL FILE — read VALID_OPS, validateOp structure, the GET→mutate→SET 'add' op for contrast, the catch block, and confirm nanoid/randomId import. Read any existing Lua strings in full per Pitfall 4)
    - __tests__/editRoute.test.ts (mirror the mock structure: env vars before imports, mock @upstash/redis with mockEval/mockGet, callPOST helper, beforeEach reset)
    - stores/useBillStore.ts (Person shape + colorIndex semantics; how IDs are generated)
    - lib/sessionSchema.ts (people[] and claims.personSlots paths for the Lua write)
    - .planning/phases/09-bill-view-redesign-identity-modal/09-RESEARCH.md (Pitfall 2, "ADD_PERSON_SCRIPT" code example, Critical Decision D, Security Domain name validation)
  </read_first>
  <behavior>
    - Test (add_person ok): POST { op:'add_person', name:'Carol' } → 200, { ok:true, personId:<string> }; redis.eval called exactly once
    - Test (name validation empty): { op:'add_person', name:'' } → 400; eval NOT called
    - Test (name validation too long): name length 51 → 400; eval NOT called
    - Test (name trimmed): name '  Carol  ' → eval ARGV[0] === 'Carol'
    - Test (session full): eval resolves 'session_full' → 409
    - Test (session not found): eval resolves 'session_not_found' → 404
    - Existing item-edit op tests remain green
  </behavior>
  <action>
    Extend VALID_OPS to include `'add_person'`.
    In validateOp add an `op === 'add_person'` block: require `typeof b.name === 'string'`,
    `b.name.trim().length >= 1`, and `b.name.trim().length <= 50`; otherwise return
    `{ ok:false, error: 'Invalid add_person: ...' }` (mirror existing validate messages).
    Add `ADD_PERSON_SCRIPT` Lua const: GET+decode guards (return 'session_not_found' / 'invalid_session');
    ensure `session.people`, `session.claims`, `session.claims.personSlots` exist; gate
    `if #session.people >= 20 then return 'session_full' end`; compute `colorIndex = #session.people % 6`;
    `table.insert(session.people, { id = newPersonId, name = name, colorIndex = colorIndex })`;
    set `session.claims.personSlots[newPersonId] = true`; SET with 'EX', 86400; return 'OK'.
    In the POST handler add an `op === 'add_person'` branch BEFORE the GET→mutate→SET path:
    generate `newPersonId` in TypeScript (nanoid/randomId), trim the name, call
    `redis.eval(ADD_PERSON_SCRIPT, [`session:${sessionId}`], [trimmedName, newPersonId])`;
    map 'session_not_found'→404, 'session_full'→409 ("Session is full (max 20 people)"),
    'invalid_session'→500; on 'OK' return `{ ok: true, personId: newPersonId }`.
    Use only current flat-schema field paths in the Lua. Write failing tests first, then implement.
  </action>
  <verify>
    <automated>npx vitest run __tests__/editRoute.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run __tests__/editRoute.test.ts` passes including add_person ok / validation / full / not-found tests
    - `grep -n "ADD_PERSON_SCRIPT" "app/api/session/[sessionId]/edit/route.ts"` returns the const and its eval call
    - add_person response body includes a non-empty `personId` string (asserted in test)
    - `grep -vE '^\s*--' "app/api/session/[sessionId]/edit/route.ts" | grep -cE "hostToken|assignedBy|editRequests"` returns 0
    - Existing item-edit op tests still pass
  </acceptance_criteria>
  <done>add_person is live: atomic person+slot creation via Lua, server-generated personId returned, name validated, 20-person cap enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/.../edit (add_person) | Untrusted name string + the request creates shared session state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | name injection in add_person | mitigate | Server-side validation: non-empty trimmed string, max 50 chars (V5 input validation); name stored as plain text, never executed |
| T-09-05 | DoS | slot exhaustion via repeated add_person | mitigate | ADD_PERSON_SCRIPT gates `#session.people >= 20` → 409; bounded growth |
| T-09-06 | Tampering | add_person append race (two devices) | mitigate | Single redis.eval Lua appends + locks slot atomically; no GET→SET window (Pitfall 2) |
| T-09-07 | Spoofing | client-supplied personId for new person | mitigate | personId generated server-side in TypeScript before eval; client cannot pre-claim an arbitrary slot |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH audit: none); no install task |
</threat_model>

<verification>
- `npx vitest run __tests__/editRoute.test.ts` green
- `npx tsc --noEmit` shows no new errors attributable to edit/route.ts
</verification>

<success_criteria>
- add_person op live, atomic, validated, capped at 20 people
- Server-generated personId returned for immediate client identity set
- No stale host fields in new Lua; existing edit-route tests still green
</success_criteria>

<output>
Create `.planning/phases/09-bill-view-redesign-identity-modal/09-02-SUMMARY.md` when done
</output>
