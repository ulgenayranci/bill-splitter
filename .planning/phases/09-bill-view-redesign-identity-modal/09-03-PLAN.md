---
phase: 09-bill-view-redesign-identity-modal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/split/PersonSlotPicker.tsx
  - components/split/IdentityModal.tsx
  - __tests__/PersonSlotPicker.test.tsx
autonomous: true
requirements: [IDENT-01, IDENT-03]
must_haves:
  truths:
    - "A 'Who are you?' dialog presents the session's people as selectable name cards"
    - "Taken names are visibly greyed out and cannot be selected"
    - "An 'I'm not listed' link reveals an inline name input and 'Add me' button inside the modal — no new screen"
    - "The modal cannot be dismissed without a selection on first load, but CAN be dismissed when changing identity"
  artifacts:
    - path: "components/split/IdentityModal.tsx"
      provides: "Dialog wrapper around PersonSlotPicker + I'm-not-listed inline add"
      contains: "Who are you?"
    - path: "components/split/PersonSlotPicker.tsx"
      provides: "modal-content slot picker with onAddPerson inline form, opacity-50 taken cards"
      contains: "onAddPerson"
  key_links:
    - from: "components/split/IdentityModal.tsx"
      to: "components/split/PersonSlotPicker.tsx"
      via: "renders PersonSlotPicker as modal content, passes onSelect + onAddPerson"
      pattern: "PersonSlotPicker"
    - from: "components/split/IdentityModal.tsx onOpenChange"
      to: "dismiss-block when allowClose is false"
      via: "controlled Dialog blocking dismissal without identity"
      pattern: "allowClose"
---

<objective>
Build the "Who are you?" identity modal (IDENT-01) by refactoring PersonSlotPicker into modal
content and wrapping it in a Dialog (IdentityModal). Add the "I'm not listed" inline-add form
(IDENT-03) and fix the pre-existing opacity test failure. This plan ships the presentational
identity surface; Plan 06 wires it into the orchestrator with real handlers.

Purpose: replace the blocking full-page slot picker with a modal that auto-shows on load and
re-opens for change-identity, without leaving the Bill View.
Output: IdentityModal.tsx (new) + refactored PersonSlotPicker.tsx with the opacity-50 fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-bill-view-redesign-identity-modal/09-CONTEXT.md
@.planning/phases/09-bill-view-redesign-identity-modal/09-PATTERNS.md
@.planning/phases/09-bill-view-redesign-identity-modal/09-UI-SPEC.md

<interfaces>
From components/ui/dialog.tsx (@base-ui/react via shadcn):
- `Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription` — DialogContent accepts `showCloseButton` prop
From components/ui/input.tsx: `Input`; from components/ui/button.tsx: `Button`
From lib/sessionSchema.ts: `PublicSessionPayload` (= SessionPayload: people[], claims.personSlots)
From stores/useBillStore.ts: `PersonId`, `AVATAR_COLORS`

Analog to copy: components/wizard/DisambiguationDialog.tsx (controlled Dialog + inner content + async submit + open-reset effect).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor PersonSlotPicker to modal content + onAddPerson inline form + opacity-50 fix</name>
  <files>components/split/PersonSlotPicker.tsx, __tests__/PersonSlotPicker.test.tsx</files>
  <read_first>
    - components/split/PersonSlotPicker.tsx (FULL FILE — keep the grid-cols-2 layout, slot cards, avatar circles, "(taken)" label, onSelect callback. Note the `opacity-40` class to change)
    - __tests__/PersonSlotPicker.test.tsx (Test 2 queries `[class*="opacity-50"]` and currently FAILS because the component uses opacity-40 — the test is ground truth; fix the component)
    - app/split/[sessionId]/CollaborativeClaimingView.tsx (the existing inline add form ~lines 482-530 — mirror it simplified to name-only, no price/qty)
    - stores/useBillStore.ts (AVATAR_COLORS lookup pattern)
    - .planning/phases/09-bill-view-redesign-identity-modal/09-UI-SPEC.md (Identity Modal Visual Contract: "I'm not listed" link style, input placeholder "Your name", maxlength 50, "Add me" button)
  </read_first>
  <behavior>
    - Test (existing Test 2, now passing): taken-name card has class containing `opacity-50`
    - Test: clicking an available name card calls onSelect with that personId
    - Test: clicking a taken name card does NOT call onSelect
    - Test: "I'm not listed" link is present; clicking it reveals an input with placeholder "Your name" and an "Add me" button
    - Test: submitting the inline add with a name calls onAddPerson with the trimmed name; empty name does not call onAddPerson
  </behavior>
  <action>
    Change the taken-card class from `opacity-40 cursor-not-allowed` to `opacity-50 cursor-not-allowed` (Pitfall 6 — test is ground truth).
    Extend the props interface with `onAddPerson?: (name: string) => Promise<void>`.
    Below the name grid, render an "I'm not listed" text link styled `text-[14px] text-amber-600 underline`;
    when tapped, reveal an inline `Input` (placeholder "Your name", maxLength 50) and a `Button`
    (`className="bg-amber-600 hover:bg-amber-700"`, label "Add me"). On submit, trim the name and, if non-empty,
    call `onAddPerson(trimmed)`; ignore empty/whitespace-only. Keep all existing grid/slot/onSelect behavior.
    Remove the outer full-page page layout wrapper so the component renders cleanly as Dialog content
    (the grid + add form only). Add the new tests to __tests__/PersonSlotPicker.test.tsx; write the failing
    tests first, then implement.
  </action>
  <verify>
    <automated>npx vitest run __tests__/PersonSlotPicker.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run __tests__/PersonSlotPicker.test.tsx` passes ALL tests including the previously-failing Test 2
    - `grep -c "opacity-40" components/split/PersonSlotPicker.tsx` returns 0
    - `grep -n "onAddPerson" components/split/PersonSlotPicker.tsx` returns the prop and its call site
    - Inline add input uses placeholder "Your name" and the link text is exactly "I'm not listed"
  </acceptance_criteria>
  <done>PersonSlotPicker is modal-ready content with the opacity-50 fix and an "I'm not listed" inline add form.</done>
</task>

<task type="auto">
  <name>Task 2: Create IdentityModal Dialog wrapper</name>
  <files>components/split/IdentityModal.tsx</files>
  <read_first>
    - components/wizard/DisambiguationDialog.tsx (FULL FILE — copy the imports, controlled Dialog pattern, the open-reset useEffect, and async submit structure)
    - components/ui/dialog.tsx (DialogContent showCloseButton prop, DialogHeader/Title/Description)
    - components/split/PersonSlotPicker.tsx (the props it now exposes after Task 1: session, onSelect, onAddPerson)
    - .planning/phases/09-bill-view-redesign-identity-modal/09-UI-SPEC.md (Identity Modal Visual Contract + Copywriting Contract: heading "Who are you?", subtext "Pick your name from the list below.")
    - .planning/phases/09-bill-view-redesign-identity-modal/09-RESEARCH.md (Pattern 1 — auto-show + change-identity; dismiss-block when no identity yet)
  </read_first>
  <action>
    Create components/split/IdentityModal.tsx as a `'use client'` component with props
    `{ open: boolean; allowClose: boolean; session: PublicSessionPayload; onSelect: (personId: PersonId) => Promise<void>; onAddPerson: (name: string) => Promise<void>; onOpenChange: (open: boolean) => void }`.
    Render a controlled `Dialog` whose `onOpenChange` BLOCKS dismissal when closing AND `allowClose === false`
    (initial identity prompt — cannot be dismissed without a selection); otherwise forwards to props.onOpenChange.
    `DialogContent` gets `showCloseButton={allowClose}`. DialogHeader: DialogTitle "Who are you?",
    DialogDescription "Pick your name from the list below." Render `<PersonSlotPicker session={session} onSelect={onSelect} onAddPerson={onAddPerson} />`
    as the body. Add an open-reset effect mirroring DisambiguationDialog so transient inline-add state clears when the modal opens.
    Do NOT add data fetching here — handlers are passed in by the orchestrator (Plan 06).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "IdentityModal" | grep -q '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Who are you?" components/split/IdentityModal.tsx` returns the DialogTitle
    - `grep -n "showCloseButton={allowClose}" components/split/IdentityModal.tsx` confirms dismiss control wiring
    - `grep -n "allowClose" components/split/IdentityModal.tsx` shows the onOpenChange dismiss-block uses it
    - `npx tsc --noEmit` reports no errors in IdentityModal.tsx
  </acceptance_criteria>
  <done>IdentityModal renders the slot picker in a Dialog with auto-show / change-identity dismiss semantics; ready for orchestrator wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → IdentityModal | User-typed name in "I'm not listed" form (validated server-side in Plan 02); identity selection is client-local intent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Spoofing | picking a taken name (identity takeover) | mitigate | Taken names are greyed (opacity-50) and non-selectable (D-01); server slot lock (Plan 02) is the authoritative guard; no takeover flow this phase |
| T-09-09 | Tampering | name input in I'm-not-listed | transfer | Length/content validation enforced server-side in Plan 02 add_person; client form only trims and gates empty |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH audit: none); shadcn dialog already present |
</threat_model>

<verification>
- `npx vitest run __tests__/PersonSlotPicker.test.tsx` green (including previously-failing Test 2)
- `npx tsc --noEmit` no errors in PersonSlotPicker.tsx / IdentityModal.tsx
</verification>

<success_criteria>
- IdentityModal + refactored PersonSlotPicker exist; opacity-50 test passes
- "I'm not listed" inline add present; dismiss-block honored when allowClose is false
</success_criteria>

<output>
Create `.planning/phases/09-bill-view-redesign-identity-modal/09-03-SUMMARY.md` when done
</output>
