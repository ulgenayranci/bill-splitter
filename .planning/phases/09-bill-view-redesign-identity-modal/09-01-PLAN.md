---
phase: 09-bill-view-redesign-identity-modal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/billMath.ts
  - __tests__/billMath.test.ts
  - app/api/session/[sessionId]/claim/route.ts
  - __tests__/sessionClaimRoute.test.ts
autonomous: true
requirements: [CLAIM-02]
must_haves:
  truths:
    - "Two people can each claim a single-quantity item without a 409 qty_exceeded error"
    - "An equal split of a price across N sharers sums back to the exact price (no lost cents)"
    - "Tapping a shared item again removes that person from the sharers"
  artifacts:
    - path: "lib/billMath.ts"
      provides: "computeEqualShareCents largest-remainder helper"
      contains: "export function computeEqualShareCents"
    - path: "app/api/session/[sessionId]/claim/route.ts"
      provides: "share action with SHARE_CLAIM_SCRIPT Lua (no bounds check)"
      contains: "SHARE_CLAIM_SCRIPT"
  key_links:
    - from: "app/api/session/[sessionId]/claim/route.ts"
      to: "redis.eval"
      via: "SHARE_CLAIM_SCRIPT for action==='share'"
      pattern: "action === 'share'"
    - from: "lib/billMath.ts computeEqualShareCents"
      to: "components/split/ClaimableItemCard.tsx (Plan 05) + results (Phase 10)"
      via: "exported helper import"
      pattern: "computeEqualShareCents"
---

<objective>
Add the integer-cents equal-split math helper and the new `share` claim action that lets
multiple people tap-to-join a single-quantity item (D-13). This is the data/logic foundation
for CLAIM-02 — both the card UI (Plan 05) and the orchestrator (Plan 06) consume these.

Purpose: tap-to-join breaks the existing `qty` bounds check (Pitfall 1); a new bounds-check-free
`share` Lua action is required, and equal splits must conserve cents exactly (Pitfall 3).
Output: `computeEqualShareCents` helper + `share` action on the claim route, both with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-bill-view-redesign-identity-modal/09-CONTEXT.md
@.planning/phases/09-bill-view-redesign-identity-modal/09-RESEARCH.md
@.planning/phases/09-bill-view-redesign-identity-modal/09-PATTERNS.md

<interfaces>
From lib/sessionSchema.ts:
- `ClaimEntry = { qty: number }` (UNCHANGED — share = each sharer holds { qty: 1 })
- `SessionClaims.items: Record<ItemId, Record<PersonId, ClaimEntry>>`

From stores/useBillStore.ts:
- `type PersonId = string`, `type ItemId = string`

Existing claim route actions: `'qty'` (bounds-checked) and `'slot'` (slot lock). This plan adds `'share'`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add computeEqualShareCents largest-remainder helper</name>
  <files>lib/billMath.ts, __tests__/billMath.test.ts</files>
  <read_first>
    - lib/billMath.ts (see existing largest-remainder block in computePersonTotals ~lines 49-58, and computePersonShareFromClaims ~line 126 — the new function goes after it and is ADDITIVE, do not modify existing functions)
    - __tests__/billMath.test.ts (mirror the existing describe/it structure; the 3-way proportional test at ~line 201 expects 333 and MUST remain green — the new helper is separate)
    - .planning/phases/09-bill-view-redesign-identity-modal/09-RESEARCH.md (Pattern 4, Pitfall 3, Critical Decision C — determinism rule)
  </read_first>
  <behavior>
    - Test: computeEqualShareCents(1000, 2, 0) === 500 and (1000, 2, 1) === 500
    - Test: computeEqualShareCents(1000, 3, 0) === 334; (1000, 3, 1) === 333; (1000, 3, 2) === 333
    - Test (sum conservation): for priceCents=1000 and numSharers=3, sum over myIndex 0..2 === 1000 exactly
    - Test (general sum conservation): for several priceCents/N pairs, the reduce over all indices always equals priceCents
    - Test: numSharers <= 0 returns 0 (guard)
  </behavior>
  <action>
    Add an exported function `computeEqualShareCents(priceCents: number, numSharers: number, myIndex: number): number`
    to lib/billMath.ts AFTER `computePersonShareFromClaims` (additive — touch no existing function).
    Implementation: guard `numSharers <= 0` returns 0; `base = Math.floor(priceCents / numSharers)`;
    `remainder = priceCents % numSharers`; return `base + (myIndex < remainder ? 1 : 0)`.
    Document the determinism rule in the JSDoc: the CALLER must sort claimant personIds lexicographically
    ascending before assigning myIndex (lowest personId gets the extra cent). Add the `describe('computeEqualShareCents', ...)`
    block to __tests__/billMath.test.ts with the cases in <behavior>. Write the failing tests first, then implement.
  </action>
  <verify>
    <automated>npx vitest run __tests__/billMath.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run __tests__/billMath.test.ts` passes including the new computeEqualShareCents block
    - The pre-existing 3-way proportional test (expects 333) still passes (helper is additive, not a replacement)
    - `grep -n "export function computeEqualShareCents" lib/billMath.ts` returns one match
  </acceptance_criteria>
  <done>computeEqualShareCents exists, is exported, sum-conservation tests pass, existing billMath tests unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 'share' action + SHARE_CLAIM_SCRIPT to the claim route</name>
  <files>app/api/session/[sessionId]/claim/route.ts, __tests__/sessionClaimRoute.test.ts</files>
  <read_first>
    - app/api/session/[sessionId]/claim/route.ts (FULL FILE — read the existing QTY_CLAIM_SCRIPT and SLOT_CLAIM_SCRIPT Lua strings, the ClaimBody type, validateBody, and the POST dispatch. Lua strings are opaque to TypeScript — read them in full per Pitfall 4 before writing a new one)
    - __tests__/sessionClaimRoute.test.ts (mirror the mock structure: env vars before imports, mock @upstash/redis, callPOSTWithParams helper, assert mockEval call args)
    - lib/sessionSchema.ts (confirm claims.items shape so the Lua field paths match the CURRENT flat schema — no hostToken/assignedBy references)
    - .planning/phases/09-bill-view-redesign-identity-modal/09-RESEARCH.md (Pitfall 1, Pitfall 4, "SHARE_CLAIM_SCRIPT" code example)
  </read_first>
  <behavior>
    - Test (share join): POST { personId:'p1', itemId:'i1', action:'share', joining:true } → 200, { ok:true }; redis.eval called with keys ['session:test-session'] and ARGV ['i1','p1','true']
    - Test (share leave): POST { ..., joining:false } → 200; ARGV third element is 'false'
    - Test (validation): action:'share' without a boolean `joining` → 400
    - Test (session not found): eval resolves 'session_not_found' → 404
    - Test (invalid session): eval resolves 'invalid_session' → 500
    - Existing 'qty' and 'slot' tests remain green
  </behavior>
  <action>
    Extend ClaimBody to `action: 'qty' | 'slot' | 'share'` and add `joining?: boolean`.
    Add `SHARE_CLAIM_SCRIPT` as a Lua const derived from QTY_CLAIM_SCRIPT but with NO bounds check:
    GET+decode guards identical (return 'session_not_found' / 'invalid_session'); ensure
    `session.claims.items[itemId]` exists; if `joining=='true'` set `session.claims.items[itemId][personId] = { qty = 1 }`,
    else set it to nil; clean up the item entry if it becomes empty; SET with 'EX', 86400; return 'OK'.
    Use only field paths present in the current flat schema (claims.items / personId / qty) — no host fields.
    In validateBody, accept 'share' and require `typeof joining === 'boolean'` and a present `itemId` when action==='share'.
    Add a dispatch branch: `if (action === 'share')` → `redis.eval(SHARE_CLAIM_SCRIPT, [`session:${sessionId}`], [itemId, personId, String(joining)])`,
    map 'session_not_found'→404 and 'invalid_session'→500 (same pattern as existing branches), then return `{ ok: true }`.
    Write the failing tests in __tests__/sessionClaimRoute.test.ts first, then implement.
  </action>
  <verify>
    <automated>npx vitest run __tests__/sessionClaimRoute.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run __tests__/sessionClaimRoute.test.ts` passes including the new share-join, share-leave, and validation tests
    - `grep -n "SHARE_CLAIM_SCRIPT" "app/api/session/[sessionId]/claim/route.ts"` returns the const definition and its eval call site
    - `grep -vE '^\s*--' "app/api/session/[sessionId]/claim/route.ts" | grep -cE "hostToken|assignedBy|editRequests"` returns 0 (no stale Phase-8 host fields in any new Lua)
    - Existing qty and slot action tests still pass
  </acceptance_criteria>
  <done>The share action is live, bounds-check-free, atomic via Lua, and validated; two people can hold qty:1 on a qty:1 item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/.../claim | Untrusted JSON body (personId, itemId, action, joining) crosses into Redis state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | share action Lua write | mitigate | All writes via single redis.eval (Lua) — atomic, no read-modify-write race (Phase 6 decision: multi() not atomic on Upstash REST) |
| T-09-02 | DoS | join-spam on one item | accept | `session.claims.items[itemId][personId] = {qty:1}` is idempotent — repeated joins by the same person are no-ops; anonymous 24h-TTL session, low value |
| T-09-03 | Tampering | malformed action/joining body | mitigate | validateBody rejects unknown action and non-boolean joining with 400 before any eval |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH Package Legitimacy Audit: none); no install task |
</threat_model>

<verification>
- `npx vitest run __tests__/billMath.test.ts __tests__/sessionClaimRoute.test.ts` green
- No new TypeScript errors: `npx tsc --noEmit` shows no new errors attributable to these files
</verification>

<success_criteria>
- computeEqualShareCents exported, sum-conserving, tested
- share action live with bounds-check-free Lua, validated, tested
- No stale host fields in new Lua; existing claim-route tests still green
</success_criteria>

<output>
Create `.planning/phases/09-bill-view-redesign-identity-modal/09-01-SUMMARY.md` when done
</output>
