---
phase: 11-bug-fixes-polish-bill-results-screens-participant-management
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/split/BillViewHeader.tsx
  - __tests__/BillViewHeader.test.tsx
autonomous: true
requirements: [HEADER-01, HEADER-02]
must_haves:
  truths:
    - "The Receipt button is gone from the bill view header (no element with aria-label View receipt) (D-01)"
    - "The Share button is a ≥44px tap target with visible presence (icon + label), easy to identify and tap on mobile (D-02)"
  artifacts:
    - path: "components/split/BillViewHeader.tsx"
      provides: "Header with Receipt button removed and enlarged Share button"
      contains: "min-h-[44px]"
  key_links:
    - from: "components/split/BillViewHeader.tsx Share button"
      to: "handleShare"
      via: "onClick"
      pattern: "onClick=\\{handleShare\\}"
---

<objective>
Implement D-01 and D-02 on the bill view header: remove the non-functional Receipt button entirely (it is a no-op, and the scanned image is never persisted to the shared session — a real "view receipt for all" feature is deferred), and enlarge the Share button to a proper ≥44px tap target with real visual presence.

Purpose: The Receipt button misleads users (does nothing) and both buttons are tiny bare icons. This plan removes the dead control and makes Share easy to find and tap on mobile.

Output: Updated `BillViewHeader.tsx` + updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-bug-fixes-polish-bill-results-screens-participant-management/11-CONTEXT.md
@.planning/phases/11-bug-fixes-polish-bill-results-screens-participant-management/11-RESEARCH.md
@.planning/phases/11-bug-fixes-polish-bill-results-screens-participant-management/11-PATTERNS.md

<interfaces>
<!-- Extracted from source — executor uses these directly. -->

Import (line 4): import { Receipt, Share2, Copy, Check } from 'lucide-react'
  → remove Receipt (and Copy if it becomes unused after edits — verify before removing Copy)

Receipt button (lines 121-127): <button aria-label="View receipt" ...><Receipt size={22}/></button>  → DELETE entirely

Share button (lines 128-139): currently bare icon, classes "text-zinc-500 hover:text-zinc-700 transition-colors", renders Check or Share2 at size 22, onClick={handleShare}, aria-label="Share bill link"
  → target: flex min-h-[44px] min-w-[44px] items-center justify-center gap-2 rounded-lg bg-amber-600 px-4 text-white hover:bg-amber-700, icon size 20, plus a text label span ("Share" / "Copied!")

Existing test: __tests__/BillViewHeader.test.tsx Test 7 (lines 117-128) asserts getByLabelText('View receipt') present → must become a NOT-present assertion
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Receipt button and enlarge Share button in BillViewHeader, update tests</name>
  <files>components/split/BillViewHeader.tsx, __tests__/BillViewHeader.test.tsx</files>
  <read_first>
    - components/split/BillViewHeader.tsx (the file being modified — import line 4, Receipt button lines 121-127, Share button lines 128-139, handleShare handler)
    - __tests__/BillViewHeader.test.tsx (the file being modified — Test 7 lines 117-128, and the getByLabelText render pattern in Tests 1-9)
    - .planning/phases/11-bug-fixes-polish-bill-results-screens-participant-management/11-PATTERNS.md (BillViewHeader pattern assignment — exact before/after JSX for both buttons)
  </read_first>
  <action>
    D-01: Delete the entire Receipt <button> block (aria-label "View receipt", lines 121-127). Remove `Receipt` from the lucide-react import on line 4. After removing, confirm whether `Copy` is still referenced anywhere in the file — if it is now unused, remove it too; otherwise leave it. Do not remove Share2 or Check (still used by the Share button).

    D-02: Restyle the existing Share <button> (lines 128-139) to be a real tap target. Keep onClick={handleShare} and aria-label="Share bill link" unchanged. Apply classes: `flex min-h-[44px] min-w-[44px] items-center justify-center gap-2 rounded-lg bg-amber-600 px-4 text-white hover:bg-amber-700 transition-colors`. Render the icon at size 20 (Check when copied, else Share2, both aria-hidden) followed by a text label span (className text-[14px] font-medium) reading "Copied!" when copied else "Share". The aria-label stays "Share bill link" so the existing accessible name is preserved.

    Update __tests__/BillViewHeader.test.tsx:
    - Replace Test 7 (lines 117-128): assert the receipt affordance is NOT present — `screen.queryByLabelText('View receipt')` is null. Keep the existing render-call shape (props: session, myPersonId, onStripTap, sessionId) used by the surrounding tests.
    - Add a new test (D-02): render the header, get the Share button via getByLabelText('Share bill link'), assert its className contains 'min-h-[44px]'.
  </action>
  <verify>
    <automated>npx vitest run __tests__/BillViewHeader.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - BillViewHeader.tsx no longer imports Receipt — `grep -c "Receipt" components/split/BillViewHeader.tsx` returns 0
    - No element with aria-label "View receipt" remains — `grep -c "View receipt" components/split/BillViewHeader.tsx` returns 0
    - Share button has the 44px target class — `grep -c "min-h-\[44px\]" components/split/BillViewHeader.tsx` returns ≥ 1
    - Share button still uses onClick={handleShare} and aria-label="Share bill link"
    - Test 7 now asserts queryByLabelText('View receipt') is null; a new test asserts the Share button className contains 'min-h-[44px]'
    - `npx vitest run __tests__/BillViewHeader.test.tsx` exits 0
  </acceptance_criteria>
  <done>Receipt button removed (D-01), Share button enlarged to ≥44px with label (D-02); tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user tap → handleShare | Client-only clipboard/share action; copies the existing share URL — no server write, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Information Disclosure | share link copied via Share button | accept | INTENDED — sharing the session join link is the core collaborative feature (CLAIM-06); link is an unguessable nanoid, 24h TTL |

Note: This plan is pure JSX/styling change with no new server surface, no new input, and no new data flow. The removed Receipt button was a no-op; removing it reduces (not increases) attack surface.
</threat_model>

<verification>
- `npx vitest run __tests__/BillViewHeader.test.tsx` green
- `npx tsc --noEmit` clean for BillViewHeader.tsx (no dangling Receipt import)
</verification>

<success_criteria>
- Receipt button + import removed (D-01)
- Share button is a ≥44px labeled tap target (D-02)
- Tests green
</success_criteria>

<output>
Create `.planning/phases/11-bug-fixes-polish-bill-results-screens-participant-management/11-03-SUMMARY.md` when done
</output>
