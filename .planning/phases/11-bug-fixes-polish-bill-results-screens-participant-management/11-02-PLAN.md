---
phase: 11-bug-fixes-polish-bill-results-screens-participant-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sessionUtils.ts
  - components/split/PersonResultsScreen.tsx
  - __tests__/PersonResultsScreen.test.tsx
autonomous: true
requirements: [RESULTS-05, TIP-03, CURR-04]
must_haves:
  truths:
    - "When the bill is not fully claimed, the Results screen shows an Unclaimed items section listing the items still needing an owner"
    - "The Results headline reads a playful up-for-grabs message when items are unclaimed, and a positive all-set message when fully claimed"
    - "Add a tip is rendered as a prominent Button, not a faint underlined text link"
    - "The currency <select> control is absent from the Results screen; the detected currency symbol still displays on amounts"
  artifacts:
    - path: "lib/sessionUtils.ts"
      provides: "Shared getUnclaimedCounts + getUnclaimedItems helpers"
      exports: ["getUnclaimedCounts", "getUnclaimedItems"]
    - path: "components/split/PersonResultsScreen.tsx"
      provides: "Unclaimed section, conditional headline, tip Button, currency select removed"
      contains: "getUnclaimedCounts"
  key_links:
    - from: "components/split/PersonResultsScreen.tsx"
      to: "lib/sessionUtils.ts"
      via: "import getUnclaimedCounts, getUnclaimedItems"
      pattern: "from ['\"].*sessionUtils['\"]"
---

<objective>
Polish the Results screen per D-03, D-04, D-08, D-09. Add an "Unclaimed items" section and a conditional playful/positive headline driven by unclaimed detection; promote the tip link to a prominent Button; remove the currency `<select>` (keeping the detected-currency display intact). Extract `getUnclaimedCounts` (plus a new `getUnclaimedItems`) into `lib/sessionUtils.ts` so three consumers share one implementation (research open-question #1 — extract to avoid a third copy).

Purpose: The Results screen is the moment of truth. It must surface unallocated items prominently and stop falsely claiming "You're all set!" when items remain. The currency dropdown reads like a converter and confuses intent, so it is removed.

Output: New `lib/sessionUtils.ts`; restyled/restructured `PersonResultsScreen.tsx`; expanded tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-bug-fixes-polish-bill-results-screens-participant-management/11-CONTEXT.md
@.planning/phases/11-bug-fixes-polish-bill-results-screens-participant-management/11-RESEARCH.md
@.planning/phases/11-bug-fixes-polish-bill-results-screens-participant-management/11-PATTERNS.md

<interfaces>
<!-- Extracted from source — executor uses these directly. -->

getUnclaimedCounts (currently duplicated in CollaborativeClaimingView.tsx:56-64 and UnclaimedBanner.tsx:10-21):
  function getUnclaimedCounts(session): { unclaimed: number; total: number }
  — iterates session.items; for each, entries = session.claims?.items?.[item.id] ?? {};
    totalClaimed = Object.values(entries).reduce((s,e)=>s+(e?.qty??0),0);
    if totalClaimed < (item.quantity ?? 1) unclaimed++

getUnclaimedItems (new — same traversal, returns filtered Item[]):
  function getUnclaimedItems(session): Item[]  // items where totalClaimed < quantity

PersonResultsScreenProps (PersonResultsScreen.tsx:28-36) — REMOVE onCurrencyChange:
  { session, personId, currencyCode, onAddTip, onEditBill, onCurrencyChange (DELETE), sessionId }

Existing headline (line 134): <h1 ...>You&rsquo;re all set!</h1>
Currency select block: lines 259-274 (DELETE) + handleCurrencyChange lines 125-127 + COMMON_CURRENCIES line 26 + currencyOptions lines 62-64
Tip link: lines 276-283 (REPLACE with shadcn Button, already imported line 7)
formatCents(cents, currencyCode) from lib/billMath.ts — still used for all amounts (D-09 keeps display)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract getUnclaimedCounts + getUnclaimedItems into lib/sessionUtils.ts and rewire existing consumers</name>
  <files>lib/sessionUtils.ts, components/split/UnclaimedBanner.tsx, app/split/[sessionId]/CollaborativeClaimingView.tsx</files>
  <read_first>
    - components/split/UnclaimedBanner.tsx (lines 10-21 — current getUnclaimedCounts implementation, source of truth for the extraction)
    - app/split/[sessionId]/CollaborativeClaimingView.tsx (lines 56-64 — duplicate getUnclaimedCounts to replace with an import)
    - lib/sessionSchema.ts (SessionPayload / PublicSessionPayload and Item types for the helper signatures)
    - .planning/phases/11-bug-fixes-polish-bill-results-screens-participant-management/11-RESEARCH.md (section 5 — unclaimed detection reuse, includes getUnclaimedItems body)
  </read_first>
  <action>
    Create `lib/sessionUtils.ts` exporting two pure functions typed against the session payload type already used by UnclaimedBanner/CollaborativeClaimingView (PublicSessionPayload or SessionPayload — match whichever the existing getUnclaimedCounts uses so all three call sites typecheck):
    - getUnclaimedCounts(session): { unclaimed: number; total: number } — copy the existing 7-line body verbatim (entries = session.claims?.items?.[item.id] ?? {}; totalClaimed via Object.values(...).reduce; compare against item.quantity ?? 1).
    - getUnclaimedItems(session): Item[] — same traversal, return session.items.filter(...) where totalClaimed < (item.quantity ?? 1).

    Replace the inline getUnclaimedCounts in UnclaimedBanner.tsx with an import from '@/lib/sessionUtils' (or the project's existing alias for lib) and delete the local definition.
    Replace the inline getUnclaimedCounts in CollaborativeClaimingView.tsx with the same import and delete the local definition. Do NOT change any other logic in CollaborativeClaimingView in this task — only the helper source. (Note: 11-04 also edits CollaborativeClaimingView in a later wave; this plan touches only the getUnclaimedCounts definition removal + import, which is wave-1-safe because 11-04 depends on this plan.)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "sessionUtils\|UnclaimedBanner\|CollaborativeClaimingView" | grep -qx 0 && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - lib/sessionUtils.ts exists and exports both getUnclaimedCounts and getUnclaimedItems — `grep -c "export function getUnclaimedCounts\|export function getUnclaimedItems" lib/sessionUtils.ts` returns 2
    - UnclaimedBanner.tsx no longer defines getUnclaimedCounts locally and imports it from sessionUtils — `grep -c "function getUnclaimedCounts" components/split/UnclaimedBanner.tsx` returns 0
    - CollaborativeClaimingView.tsx imports getUnclaimedCounts from sessionUtils — `grep -c "sessionUtils" "app/split/[sessionId]/CollaborativeClaimingView.tsx"` returns ≥ 1
    - `npx tsc --noEmit` reports no errors in the three touched files
  </acceptance_criteria>
  <done>Single shared helper module; both existing consumers import it; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure PersonResultsScreen — unclaimed section, headline, tip Button, remove currency select</name>
  <files>components/split/PersonResultsScreen.tsx, __tests__/PersonResultsScreen.test.tsx</files>
  <read_first>
    - components/split/PersonResultsScreen.tsx (the file being modified — headline line 134, currency select lines 259-274, handleCurrencyChange lines 125-127, COMMON_CURRENCIES line 26, currencyOptions lines 62-64, tip link lines 276-283, props interface lines 28-36, Button import line 7)
    - lib/sessionUtils.ts (the helpers created in Task 1 — getUnclaimedCounts, getUnclaimedItems)
    - components/split/UnclaimedBanner.tsx (amber styling reference for the unclaimed section)
    - __tests__/PersonResultsScreen.test.tsx (makeSession fixture lines 13-36, defaultProps lines 38-45 — onCurrencyChange line to delete)
    - .planning/phases/11-bug-fixes-polish-bill-results-screens-participant-management/11-PATTERNS.md (PersonResultsScreen pattern assignment — exact JSX targets for headline, unclaimed section, tip Button, deletions)
  </read_first>
  <action>
    Import getUnclaimedCounts and getUnclaimedItems from '@/lib/sessionUtils'. Compute `const { unclaimed: unclaimedCount } = getUnclaimedCounts(session)` near the top of the component body.

    D-04 headline (replace line 134): render conditionally — when unclaimedCount > 0, a playful message `Hold up — ${unclaimedCount} item${unclaimedCount === 1 ? '' : 's'} still up for grabs!`; otherwise keep `You're all set!`. Final playful wording is Claude's discretion within the playful tone (D-04), but it MUST contain the substring "up for grabs" so the test anchor is stable, and the fully-claimed branch MUST contain "all set".

    D-03 unclaimed section (new, placed directly after the headline): when unclaimedCount > 0, render a labeled section containing the literal text "Unclaimed items" and a list of getUnclaimedItems(session) rendered by item.name. Style it as an amber callout mirroring UnclaimedBanner (rounded, bg-amber-50, border-amber-200, amber text). When unclaimedCount === 0, render nothing.

    D-08 tip Button (replace the link at lines 276-283): use the already-imported shadcn Button with variant="outline" and amber styling (border-amber-600 text-amber-600 hover:bg-amber-50), onClick={onAddTip}, label "Add a tip". It must be an actual <Button> (role button), not an anchor or underlined span.

    D-09 currency removal: delete the entire currency <select> block (lines 259-274), the handleCurrencyChange function (lines 125-127), the COMMON_CURRENCIES constant (line 26), and the currencyOptions derivation (lines 62-64). Remove onCurrencyChange from PersonResultsScreenProps (lines 28-36) and from the function parameter destructuring. Keep currencyCode in props and keep all formatCents(amount, currencyCode) call sites unchanged — the detected currency must still display everywhere.

    Update __tests__/PersonResultsScreen.test.tsx: delete the onCurrencyChange line from defaultProps (line ~43). Add tests per the behavior list below using makeSession with a partially-unclaimed claims object (e.g. an item with quantity 2 but only qty 1 claimed).
  </action>
  <verify>
    <automated>npx vitest run __tests__/PersonResultsScreen.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - PersonResultsScreen.tsx imports getUnclaimedCounts/getUnclaimedItems from sessionUtils — `grep -c "sessionUtils" components/split/PersonResultsScreen.tsx` returns ≥ 1
    - onCurrencyChange removed from props and signature — `grep -c "onCurrencyChange" components/split/PersonResultsScreen.tsx` returns 0
    - currency <select> and handleCurrencyChange removed — `grep -c "currency-select\|handleCurrencyChange\|COMMON_CURRENCIES" components/split/PersonResultsScreen.tsx` returns 0
    - Headline includes both branches — `grep -c "up for grabs" components/split/PersonResultsScreen.tsx` returns ≥ 1 and `grep -c "all set" components/split/PersonResultsScreen.tsx` returns ≥ 1
    - Tip affordance is a Button labeled "Add a tip" (test asserts getByRole('button', { name: /Add a tip/i }) is defined)
    - Tests assert: "Unclaimed items" text present + unclaimed item name shown when partially claimed (D-03); headline matches /up for grabs/i when unclaimed and /all set/i when fully claimed (D-04); currency combobox/select absent — queryByRole('combobox') is null (D-09); tip Button present (D-08)
    - `npx vitest run __tests__/PersonResultsScreen.test.tsx` exits 0
  </acceptance_criteria>
  <done>Results screen surfaces unclaimed items + correct headline, prominent tip Button, no currency select; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SWR cache → PersonResultsScreen render | Session data already fetched/validated server-side; component only reads and renders it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering | item name rendering in unclaimed list | mitigate | React renders item.name as a text node (JSX escape); no dangerouslySetInnerHTML — XSS via crafted item names structurally prevented |
| T-11-07 | Information Disclosure | unclaimed items exposed on Results | accept | INTENDED — surfacing unclaimed items is the feature (D-03); session data is already visible to all participants by design |

Note: This plan is UI-only and removes a write control (currency select); it introduces no new server surface. The currency write op (update_currency) is intentionally preserved server-side per D-09 but no longer reachable from this screen.
</threat_model>

<verification>
- `npx vitest run __tests__/PersonResultsScreen.test.tsx` green
- `npx tsc --noEmit` clean for the three touched files
- Detected currency symbol still rendered on amounts (formatCents call sites unchanged)
</verification>

<success_criteria>
- Shared sessionUtils helpers serve all three consumers (D-03 infra)
- Unclaimed items section + conditional headline (D-03, D-04)
- Prominent tip Button (D-08)
- Currency select removed, display intact (D-09)
- All PersonResultsScreen tests green
</success_criteria>

<output>
Create `.planning/phases/11-bug-fixes-polish-bill-results-screens-participant-management/11-02-SUMMARY.md` when done
</output>
